# BScout — MASTERPLAN

> **Единственный источник задач и прогресса проекта.** Не создавайте параллельных
> `PLAN_*.md` / `TODO_*.md` — весь трекинг живёт здесь.
> Полная карта всех блоков (включая архивные) — [INDEX.md](./INDEX.md).
> Что автотестами не подтвердить — [MANUAL_VERIFICATION.md](./MANUAL_VERIFICATION.md).
> Старшинство планов и краткий протокол — в корневом `CLAUDE.md`.

## Текущий статус (сводка)

| Поле | Значение |
|------|----------|
| Активный блок | **08 — боевой обход магазинов** (P0: нужен контролируемый полный прогон четырёх доступных источников) |
| Стадия | инженерно почти прод · по продукту предзапуск, упирается в **живые данные** и **приём платежей** |
| Режим | разработка · ветка `feature/backend-core` (фронт влит туда же) |
| Проверено прогоном 2026-07-29 | **1007 тестов зелёные**, ruff чист, фронт `lint` + `build` чисто (**18 маршрутов**), четыре доступных парсера прошли ограниченный live-smoke по 3/3 валидных товара, ARQ healthcheck успешен |
| Готово | 01–03, 05, 06, 09–13, 16, **20** мониторинг во фронте · **19** аккаунт/соцвход/аудит · **15** [~] эксплуатация · **17** матчинг v2 (Фазы 2–5) |
| Замер 2026-08-08 | Первые настоящие живые прогоны, лимит 50 на магазин, `skipped: 0` везде. `auto`: **Profi с `section=zapchasti` 50/50 (100%)**, Profi без фильтра 22/50, Divizion с фильтром 41/44, Liberti 13/50, **TGSM 0/50**. Офферов в БД **402 → 624**, из них живых (реальный скрейп) **316**, демо-сида 308: живые — divizion 138, profi 78, liberti 50, tgsm 50; ГринСпарк живого парсера не имеет |
| Ключевые дыры (P0) | **живые цены неполны** — в БД 316 живых офферов и 308 демо; ограниченные прогоны по четырём доступным магазинам проведены 2026-08-08, но **полного контролируемого обхода всё ещё не было** (Блок 08) · **деньги принять нельзя** — подпись вебхука ЮKassa заглушка, 54-ФЗ и возвраты нет (Блок 12) · **уведомления никуда не доставляются** — канал не выбран, только in-app/лог (Блок 13) · **восстановление пароля и соцвход не подключены к фронту** (Блок 19) |
| Частично | **04** Docker (образ фронта ни разу не собирался; контейнер бэкенда отстал от кода на 6 маршрутов — пересобрать), **03** фронт-Vitest, **17** петля gaps→автокандидаты, **18** мультимодельность не начата |
| Обновлено | 2026-08-08 |

## Легенда статусов

`[ ]` не начато · `[~]` в процессе · `[x]` выполнено · `[!]` заблокировано · `[-]` отменено

## Протокол агента

**Холодный старт (каждая сессия):**
1. Прочитать сводку статуса выше — или `bash scripts/plan/plan_status.sh` — плюс `git log --oneline -10` и `git status`.
2. Найти первый незакрытый `[ ]`; это следующий шаг. Доложить пользователю активный блок и шаг.
3. **Не** перечитывать план целиком: детали закрытых блоков лежат в `docs/plan/archive/` и поднимаются по требованию (`restore_block.sh`).

**Во время работы:**
4. Один агент = один блок. Крупные рискованные проходы — отдельной сессией, не параллельно.
5. Параллельные агенты работают в изолированных worktree `.claude/worktrees/<block-slug>`, чтобы правки не перемешивались.
6. Что нельзя подтвердить автотестом (у проекта тестов нет) — заносить в `MANUAL_VERIFICATION.md` со статусом ⬜.
7. Правьте только свой блок и его строку в `INDEX.md` — так план не становится точкой гонки между агентами.

**Закрытие (обязательно перед концом сессии):**
8. Выполненный пункт: `[ ]` → `[x] YYYY-MM-DD`. Обновить «Текущий статус» и «Историю обновлений».
9. Блок закрыт целиком → `bash scripts/plan/archive_block.sh NN` (уносит секцию в `archive/`, флипает строку в `INDEX.md`).
10. Отдельный коммит на блок: `docs(plan): mark NN done — <результат>` (коммитить только по явной просьбе — см. `CLAUDE.md`).

---

<!-- BLOCKS:START — всё между этими маркерами обслуживают scripts/plan/*.sh; не перемещайте и не удаляйте маркеры -->

## БЛОК 01 — Интеграция фронтенда с бэкендом (auth, кабинет, подписка)

**Статус:** [~] · **Заведён:** 2026-07-25 · **Ветка:** `feature/frontend-integration`

**Проблема.** Data-слой фронта (`models/{auth,user,payment}/` — Zod-схемы, axios-сервисы, React Query хуки), axios с инъекцией JWT + авто-refresh, `next.config.mjs`-прокси `/api → :8000` и бэковые эндпоинты auth — всё написано и по контрактам совместимо (JSON = snake_case, совпадает с Zod). Но «последняя миля» не сделана: формы login/register — статичные `<form>` без `onSubmit`/state; ни один хук `models/*` не вызывается в `app/`; Effector-сторы `$isAuth`/`$user` нигде не используются; страницы кабинета/подписки показывают захардкоженные данные. Задача — соединить готовый data-слой с готовой вёрсткой.

### Фаза 0 — верификация бэка *(готово)*
- [x] 2026-07-25 Модели, миграции, старт uvicorn проверены — бэк исправен (мнимый баг `indexed=True` не подтвердился, в коде `index=True`).
- [x] 2026-07-25 Auth-flow подтверждён руками: `POST /api/auth/register` → 201, `/login` → 200, `GET /api/users/me` с Bearer → 200. JSON = snake_case. `/users/me/subscription` без подписки → **404** (учесть в кабинете).

> **Статус реализации (2026-07-25):** весь код Фаз 1–4 написан и прошёл адверсариальное статическое ревью 6 агентами (типы/компиляция и контракты — 0 находок; финал: «ship-with-nits»). ⚠️ **Frontend НЕ собран:** `npm install` заблокирован (нет сети/permission), `node_modules` отсутствуют → `npm run build/lint` и frontend-тесты в этой сессии не запускались. Код помечен `[x]`, но DoD-сборка — `[!]` (см. ниже).

### Фаза 1 — авторизация end-to-end
- [x] 2026-07-25 `app/login/page.tsx` → react-hook-form + `loginRequestSchema` + `useLogin()`, обработка ошибки 401, редирект на `/account`.
- [x] 2026-07-25 `app/register/page.tsx` → rhf + клиентская схема поверх `registerRequestSchema` (confirm-пароль + согласие только на клиенте; пароль min 6; отправляются только контрактные поля). Авто-вход после регистрации.
- [x] 2026-07-25 Auth-состояние: стор переписан (`$isAuth`/`$authReady`, `setAuth`/`authHydrated`), новый `AuthProvider` гидратит из localStorage + кросс-таб `storage`-sync; `useLogin/useRegister` → `setAuth(true)`, `useLogout` → `setAuth(false)`.

### Фаза 2 — Header + защита маршрутов
- [x] 2026-07-25 `Header` переписан: сам вычисляет вход из `$isAuth`+`useMe`, реальный `useLogout()`, ссылки Кабинет/Админка; пропы `navCta/showProfileIcon/adminBadge` убраны со всех 10 страниц.
- [x] 2026-07-25 Guard `RequireAuth` (shared, чистый) на `/account`, `/subscription`; для `/admin` — новый виджет `RequireAdmin` (проверяет `is_admin`, не только вход). `failAuth()` в axios: терминальный 401 чистит токены + `setAuth(false)` + редирект.

### Фаза 3 — Личный кабинет на реальных данных
- [x] 2026-07-25 `account` на `useMe()`+`useSubscription()`+`useSearchHistory()`, состояния loading/empty, обработка 404 подписки. Фейковый блок «Недавно просмотренные» удалён (нет бэкенд-эндпоинта).
- [x] 2026-07-25 Созданы `account/settings` (правка профиля через `useUpdateMe`) и `account/history` (полная история) — битые ссылки сайдбара устранены. Общий `AccountSidebar` (usePathname) вместо 4× дублирования.

### Фаза 4 — Подписка/тарифы + оплата
- [x] 2026-07-25 `subscription` → `useSubscription()`, смена тарифа `useSubscribe()` (POST /payment/subscribe), отмена `useCancelSubscription()` с подтверждением. Фейковые «42/100» и «Visa 4242» удалены (нет бэкенда — см. вопросы).
- [x] 2026-07-25 `tariffs` → CTA зависит от входа (`$isAuth`): вошёл → `/subscription`, иначе `/register?plan=`.

### Проверка/DoD
- [!] **`npm run build && npm run lint` НЕ запущены** — заблокировано (нет `node_modules`, `npm install` отклонён). Нужно прогнать после `npm install`. Это единственный незакрытый пункт блока.
- [x] 2026-07-25 Backend-тесты: **17 passed** (unit 15 + integration 2). Попутно исправлен реальный баг тест-инфры (Блок 03): `tests/integration/conftest.py` вызывал `create_all` до импорта моделей → таблицы не создавались; добавлен `import src.main`.
- [x] 2026-07-25 Верификация — адверсариальное статическое ревью (6 агентов) вместо сборки; все находки-исправления применены (см. ниже).
- [ ] Ручные e2e-проверки через UI — занесены в `MANUAL_VERIFICATION.md` (не автотестуются; блокер — сборка + порт 8000).

**Исправлено по ревью:** `.btn-danger` добавлен в globals.css (был неопределён); `RequireAdmin` (админку видел любой залогиненный); `failAuth()` в axios (терминальный 401 не сбрасывал состояние); `<Image unoptimized>` для SVG-логотипа; alias-импорты (`payment/schema`, `app/page`); localize `Plan` в `format.ts` (убрана инверсия слоёв shared→models).

**Осознанно НЕ сделано (обоснование):** проверку exp access-токена в `AuthProvider` не добавляли — токены живут 15 мин, сброс по exp разлогинивал бы каждые 15 мин и ломал refresh; вместо этого — презенс-гидрация + серверная сверка через `failAuth`/`useMe` 401.

**Открытые вопросы (ответить в конце сессии):**
1. **Backend logout — заглушка (security).** `/auth/logout` не блэклистит refresh-токен (хотя `/refresh` blacklist проверяет) → украденный refresh валиден ~30 дней после «выхода». Фикс требует менять контракт logout (передавать refresh_token) — не делал, т.к. это backend-хардненинг вне «склейки». Делать?
2. **Способ оплаты и счётчик «использовано N/100»** — на бэке нет эндпоинтов. Фейковые данные удалил. Нужны ли эти фичи (тогда — отдельный backend-блок)?
3. **Порт 8000 занят** проектом `axidi` — фронт-прокси смотрит на `:8000`. Для e2e освободить 8000 или временно перенаправить прокси на `:8001`?
4. **Frontend-TDD (Vitest)** — не поднимал: без `node_modules` тесты не запустить. Ставить тест-раннер отдельным блоком?

**Мелкие нити (не блокеры, оставлены):** внутренние ссылки через `<a href>` вместо `<Link>` на статических mock-страницах (search/product/tariffs breadcrumbs); focus-ring `shadow-[…#141413]` хардкодом во всех инпутах (pre-existing, = токен slate); `bg-success-soft` в product (pre-existing mock, токен не определён).

## БЛОК 04 — Docker бэкенда и финализация деплоя

**Статус:** [~] 2026-07-25 · **Источник:** `docs/sprints/sprint-001-backend-api.md` §1.7 · **Код написан, сборка НЕ проверена (нет сети для `docker build`/pip).**

- [x] `backend/Dockerfile` — `python:3.11-slim` + **pip install requirements.txt** (не poetry: poetry в проекте не используется, см. CLAUDE.md) + `alembic upgrade head && uvicorn`. Плюс `backend/.dockerignore`.
- [x] Сервис `backend` в `docker-compose.yml` (`depends_on` с healthcheck postgres, env `POSTGRES_HOST=postgres`/`REDIS_HOST=redis`, порт `8000:8000`).
- [x] **Порты параметризованы (решение владельца: прод = 8000, локально — свободный).** В `docker-compose.yml` host-порты через env с прод-дефолтами: `${BACKEND_PORT:-8000}`, `${POSTGRES_HOST_PORT:-5432}`, `${REDIS_HOST_PORT:-6379}`; плюс `JWT_SECRET_KEY`/`DEBUG` из окружения (не хардкод). Локальные значения — в gitignored `bsout-pricer/.env` (**BACKEND_PORT=8010**, POSTGRES_HOST_PORT=5434); шаблон — `.env.example`. Проверено `docker compose config`: локально резолвится 8010/5434/6379, без `.env` — 8000/5432/6379.
- [x] Фронт-прокси синхронизирован: `next.config.mjs` → `process.env.BACKEND_URL ?? 'http://localhost:8000'`; локальный `frontend/.env.local` = `http://localhost:8010` (+ `frontend/.env.example`).
- [!] `docker build` + `docker compose up` — **НЕ запускались**: sandbox без сети (нельзя тянуть базовый образ и pip-пакеты). Прогнать при доступной сети.
- [ ] Прогон эндпоинтов в контейнере → `MANUAL_VERIFICATION.md` (после сборки).

**Заметки:** отклонение от плана: `poetry install` → `pip` (осознанно, poetry не установлен). Порт 8000 на хосте занят проектом `axidi` — поэтому локальный дефолт 8010; в проде маппинг остаётся 8000.

## БЛОК 07 — Пять парсеров магазинов

**Статус:** [~] РАЗБЛОКИРОВАН, идёт реализация · **Источник:** `docs/sprints/sprint-001-backend-api.md` §2.2–2.6

**Решение владельца (2026-07-25):** разведка + парсеры на фикстурах. Боевые прогоны `update_catalog` по живым каталогам **не запускаются** без отдельного согласия — это заметная нагрузка на чужие сайты.

### 🔍 Результаты разведки (2026-07-25, вежливая, с паузами)

| Магазин | Платформа | Состояние |
|---|---|---|
| ТГСМ `taggsm.ru` | OpenCart | Доступен. `itemprop="price"`. ⚠️ **Медленный и нестабильный: 2 из 3 запросов упали по таймауту 25 с** — парсеру нужны ретраи и большой timeout |
| Профи `siriust.ru` | CS-Cart | Доступен, цена в JSON-разметке (`"price": 539`) |
| Либерти `liberti.ru` | 1С-Битрикс | Доступен, цены на страницах разделов (опт + розница) |
| Дивизион `divizion126.ru` | 1С-Битрикс | Доступен, `itemprop="price"` микроразметка |
| ГринСпарк `green-spark.ru` | — | 🚫 **Отдаёт HTTP-клиентам заглушку бот-защиты** (13 КБ, `noindex, noarchive`, спиннер). В браузере открывается нормально. Обход бот-защиты не делаем |

**Что это меняет:**
- У всех четырёх доступных есть **`sitemap.xml`** (7 700–14 000 URL товаров) — перечислять товары нужно по нему, а не угадывать пагинацию. Надёжнее и легче для чужого сайта.
- YML-фидов Яндекс.Маркета по типовым путям нет ни у кого — парсинг HTML неизбежен.
- Фикстуры сохранены в `backend/tests/fixtures/parsers/` (home + product по каждому доступному магазину) — парсеры пишутся и тестируются офлайн на них.

**⚠️ ДВА ВОПРОСА ВЛАДЕЛЬЦУ:**
1. **ГринСпарк**: сайт сознательно закрыт от автоматических клиентов. Единственный честный путь — договориться с магазином о выгрузке (YML/CSV/API). Пока — парсер не пишем.
2. **ГринСпарк — московский?** На главной указан адрес «Москва, Пятницкое шоссе, ТК „Митинский радиорынок“». Проект — агрегатор **по Ставрополю**. Если у сети нет ставропольской точки, его цены в выдаче вводят пользователя в заблуждение (другой город = другие сроки и доставка). Нужно проверить и либо исключить магазин, либо явно помечать город.

**Почему пропущен (осознанно):** реализация требует (а) установки `beautifulsoup4`/`lxml`/`tenacity` — **нет сети** (`pip install` заблокирован); (б) **живых скрейпов** реальных сайтов, чтобы подобрать и проверить CSS-селекторы — тоже нет сети и нет доступа к HTML. Писать 5 парсеров «вслепую» (селекторы-догадки, без запуска и тестов) = гарантированно неверный, непроверяемый код. По твоей инструкции «если в чём-то не уверен — пометить и оставить вопрос».

- [x] Зависимости `beautifulsoup4`, `lxml`, `tenacity` добавлены в `requirements.txt` (подготовка; установятся при доступной сети).
- [x] **Четыре парсера написаны и проверены на реальном HTML магазинов** (0 и 3 живых запроса у агентов — лимит соблюдён). Разбор подтверждён вживую: ТГСМ 2000 ₽ / нет в наличии / sku 44990; Профи 539 ₽ + **опт 431 ₽** / в наличии; Дивизион 57 ₽ / в наличии; Либерти — 37 товаров с листинга раздела. Зарегистрированы в `parser_manager` через `register_default_parsers()` в `main.py`.
- [x] 2026-07-29 Ограниченный live-smoke после исправлений: **TGSM 3/3, Profi 3/3, Liberti 3/3, Divizion 3/3** по обязательным полям и цене > 0. Исправлены два live-only дефекта: Profi начинал с категорий sitemap и возвращал 0 товаров; Divizion считал `/catalog/`, поиск и служебные страницы карточками.
- [x] 314 юнит-тестов парсеров, все офлайн на фикстурах (у P1 офлайновость доказана прогоном с заблокированными сокетами).

### 🔎 Что выяснилось при реализации — влияет на продукт

1. **⚠️ ТГСМ показывает наличие ПО ФИЛИАЛАМ: 33 филиала, из них ставропольских всего 2** («Ставрополь (Лерм.)», «Ставрополь (Туха.)»). Общий флаг «в наличии» на карточке относится ко всей сети. Для **городского** агрегатора это означает: нельзя брать глобальный статус — товар может лежать в Москве и отсутствовать в Ставрополе, и пользователь поедет впустую. Парсер уже кладёт карту филиалов в `raw.branches` и ставропольские в `raw.stavropol` — **нужно решить, как это учитывать в выдаче** (отдельное поле «наличие в городе» + фильтр). Это не сделано.
2. **Оптовая цена Либерти требует авторизованного оптового кабинета** — на анонимных страницах только розница (моя вводная агенту про «опт на страницах разделов» оказалась неверной, агент проверил и поправил). Куки/сессий в каркасе нет. Нужно **решение владельца**: есть ли оптовый аккаунт, который можно использовать.
3. **Полный обход каталога — 3–4 часа на магазин** (~10.7k URL Дивизиона, ~14k Либерти, последовательно с паузой 1 с). Только ночные прогоны и/или `limit`. Это же определяет требования к Блоку 08 (планировщик).
4. У Профи ЧПУ-адреса не позволяют отличить товар от категории по URL — фильтрация чёрным списком, часть запросов уйдёт впустую. При первом боевом прогоне посмотреть долю не-товаров. Каркас (`BaseParser`, `ParseResult`, `ParserService`, upsert, `safe_request`) готов в Блоке 06 — остаётся реализовать `update_catalog()`/`search()` под каждый сайт.
5. **Расхождение discovery между `profi` и `tgsm` — осознанное, не забытый фикс.** У Профи `update_catalog(limit=…)` сортирует URL по глубине пути и разбирает батчами `max(CATALOG_CONCURRENCY, limit*2)`, пока не наберётся `limit` валидных карточек; у ТГСМ остался прямой `urls[:limit]` до разбора. Причина — разная природа фильтра из п. 4: Профи фильтрует **чёрным списком** по ЧПУ, поэтому в отфильтрованный список проходят категории и листинги, и при `limit` в выборку попадали одни они (тот самый дефект «0 товаров», починенный 2026-07-29). У ТГСМ (OpenCart) `filter_product_urls` — **белый список** по маркеру `product_id=`: `route=product/category`, `information/information`, `manufacturer/info`, статические `.html` отсеиваются до среза, так что «первые N — это категории» структурно невозможно. Вторая половина фикса Профи к ТГСМ тоже неприменима: все его адреса имеют вид `/index.php?...`, глубина пути у товара и у категории одинаковая (=1), сортировка по глубине — no-op. Инвариант закреплён тестами `test_update_catalog_limit_is_not_starved_by_category_urls` и `test_product_urls_share_one_path_depth`.
   - **Остаточный риск (принят):** `urls[:limit]` не добирает замену, если карточка не разобралась (снятый товар, таймаут) — ограниченный прогон вернёт меньше `limit`, вплоть до 0, если не разобрались все первые `limit`. В отличие от Профи это не систематическое явление: URL с `product_id=` — реальные карточки. Батчинг не портируем сознательно — ТГСМ самый медленный и нестабильный источник (см. таблицу разведки: `REQUEST_TIMEOUT=45`, `REQUEST_DELAY=1.0`, `CATALOG_CONCURRENCY=2`), и добор удвоил бы нагрузку на него ради редкого случая. Поведение зафиксировано тестом `test_update_catalog_limit_does_not_backfill_unparseable_cards` — если решение пересмотрят, тест упадёт и заставит обновить этот пункт. Долю неразобранных карточек ТГСМ посмотреть на первом боевом прогоне (там же, где долю не-товаров Профи из п. 4).

### Качество разбора — замер на реальном HTML *(2026-07-29)*

Прогон четырёх парсеров по сохранённым страницам магазинов (`tests/fixtures/parsers/`, 118 записей): **обязательные поля — 118/118** (sku, заголовок, цена > 0, `stock_status` внутри CHECK, url), то есть в БД ничего не падает. Дальше расходится по необязательным: **наличие распознано у 72%**, оптовая цена у 34%, категория у 34%.

- [ ] **Либерти: наличие не читается из листинга** (33 из 37 — `no stock info for listing item`). ⚠️ Проблема **уже**, чем записывалось раньше: замер 2026-08-08 показал, что при обходе с разбором карточек наличие определяется у **100% позиций** (50 из 50, ноль `stock_status='unknown'`), — именно так и работает `update_catalog`. Осталось проверить, остались ли вообще пути, где данные берутся из листинга, и если да — добирать наличие с карточки.
- [ ] Оптовая цена есть **только у Профи** — подтверждено замером 2026-08-08 на живых данных: среди офферов, полученных реальным скрейпом, опт заполнен у profi **78 из 78 (100%)**, у divizion **0 из 138**, liberti **0 из 50**, tgsm **0 из 50**. Ненулевые значения опта у остальных магазинов в БД принадлежат **демо-записям сида** (ссылки вида `lb…`, `dv-…`, `tg-…`), а не скрейпу; у ГринСпарка живого парсера нет вовсе. Это согласуется с п. 2 выше: у Либерти опт закрыт авторизованным оптовым кабинетом. Решение владельца прежнее: показываем ли опт вообще, раз он покрывает один магазин из пяти.
- [ ] **TGSM: фильтр по разделу структурно неприменим.** Магазин на OpenCart, все товарные ссылки имеют вид `index.php?route=product/product&product_id=N` — различать разделы подстрокой URL не в чем, поэтому параметр `section` на TGSM не действует. Следствие (замер 2026-08-08): ограниченный прогон по sitemap набирает аксессуары — гарнитуры, наушники, FM-модуляторы — и даёт **0% `auto`**, худший результат из четырёх магазинов. Нужен другой механизм отбора: обход по категориям через параметр `path=`, а не по sitemap.
- [ ] Категория заполняется только с карточек и с разделов Либерти; при обходе по sitemap у Дивизиона и Профи в листинге её нет.



## БЛОК 08 — Планировщик и очередь (arq worker)

**Статус:** [~] · **Заведён:** 2026-07-25 · **Источник:** `docs/sprints/sprint-001-backend-api.md` §2.8 · **Воркер написан, запущен и проверен вживую 2026-07-26; запуск переведён в очередь и управляем по объёму 2026-07-29. Боевого обхода магазинов не было ни разу — это и есть причина, по которой в системе нет живых цен (единственный открытый пункт блока).**

**🐞 Два дефекта, найденные при первом реальном запуске:**
1. **Детектор снижения цен никто не вызывал.** `AlertService.scan_for_drops` из Блока 13 не был подключён ни к одной задаче — `grep` по `src/` находил только определение. Уведомления, за которые берут 499 ₽, не сработали бы ни разу. Теперь вызывается после каждого обновления цен.
2. **`sync_prices` и `sync_catalog` были одной и той же функцией** (`run_all()`), при этом стояли в расписании 5 раз в сутки. С учётом находки Блока 07 (полный обход — 3–4 часа на магазин) это означало почти непрерывный обход чужих сайтов. Разведено: тяжёлый полный обход — **один раз в сутки в 03:00**, а обновление цен — только по **отслеживаемым пользователями товарам** (лимит 500 офферов за прогон, 3 раза в день). Это и дешевле для магазинов, и напрямую обслуживает платную фичу.

- [x] Зависимость `arq` в `requirements.txt`; `backend/src/worker.py` — `WorkerSettings` (RedisSettings из `settings`), функции `sync_catalog`/`sync_prices` → `parser_service.run_all()`.
- [x] Расписание: каталог ежедневно 03:00 (`cron hour=3`), цены каждые 6 ч (`cron hour={0,6,12,18}`). Приоритетные-почасовые — отложено (нет флага приоритета у offers).
- [x] Локи `parser:lock:{slug}` — наследуются из `run_one` (Блок 06).
- [x] Изоляция ошибок — `run_all` через `run_isolated` (Блок 06): падение одного парсера не роняет остальные.
- [x] `arq src.worker.WorkerSettings` **запущен** — 8 задач зарегистрировано, парсеры поднимаются в `on_startup`.
- [x] Закрыты два долга прошлых блоков: `expire_subscriptions` (ежечасно) гасит просроченные подписки — раньше их никто не гасил и админ-статистика завышала число активных; `cleanup_refresh_tokens` (ночью) чистит истёкшие и отозванные токены — таблица росла по строке на каждый логин и каждый refresh.
- [x] Проверено на живых данных: протухшая подписка погашена (`expired: 1`), мёртвые токены удалены (`deleted: 6`), живой токен не тронут. 3 теста + задачи сделаны тестируемыми (принимают сессию снаружи).
### Управление объёмом обхода и асинхронный запуск *(2026-07-29)*

**Почему понадобилось.** Анализ парсера показал, что запуск из админки был неуправляемым: `full_sync` числился в контракте с самого Блока 06 (в ревью 2026-07-25 помечен как «dead-параметр»), но `run_one` его игнорировал и всегда звал `update_catalog()` без лимита — то есть **любое нажатие «Запустить» означало полный обход на 3–4 часа**. Ни выбрать объём, ни проверить парсер на сотне позиций было нельзя.

- [x] 2026-07-29 **`full_sync` и `limit` доведены до парсера.** `resolve_catalog_limit(full_sync, limit)`: явный `limit` всегда сильнее; без него `full_sync=True` снимает потолок, `False` ставит `DEFAULT_RUN_LIMIT=500` (то же число, что у `refresh_tracked_offers`). Применённый лимит возвращается в ответе. Абстрактный `BaseParser.update_catalog(limit=None)` приведён к сигнатуре, которая уже была у всех четырёх парсеров. Ночной `sync_catalog` явно `run_all(full_sync=True)` — полный обход в 03:00 сохранён, а безопасным дефолтом стал ограниченный прогон.
- [x] 2026-07-29 Ночной полный обход защищён флагом `PARSER_FULL_SYNC_ENABLED=false`: cron остаётся наблюдаемым, но без явного opt-in не обращается к магазинам. `sync_prices` отдельно считает источники без парсера и отклонённые результаты.
- [x] 2026-07-29 Dev-worker получил ARQ healthcheck вместо HTTP-пробы backend-образа; команда `arq src.worker.WorkerSettings --check` подтверждена на живом контейнере.
- [x] 2026-07-29 **Лок больше не протухает посреди полного обхода.** `LOCK_TTL=3600` был короче самого обхода (3–4 ч): через час лок истекал, и второй запуск шёл параллельно поверх первого — двойная нагрузка на чужой магазин и гонка upsert по одному `(store_id, source_sku)`. `resolve_lock_ttl`: безлимитный прогон держит лок 6 ч, ограниченный остаётся на часе.
- [x] 2026-07-29 **Запуск переведён в очередь arq — раньше обход шёл внутри HTTP-запроса.** `POST /api/admin/parsers/run` ставит задачу и отдаёт **202** (замерено: 62 мс против 3–4 часов ожидания). До этого прогон на 500 позиций (8–10 мин) упирался в `proxy_read_timeout` nginx (дефолт 60 с) → админ видел бы 504 при успешно отработавшем парсере. Новое: `parser/service/queue.py` (пул arq, с `password` в `RedisSettings` — в почтовой очереди он потерян), `ParserService.enqueue_run`, задача `worker.run_parser`, зарегистрированная через `func(..., timeout=FULL_SYNC_LOCK_TTL)` — **дефолтный таймаут задачи arq 300 с убил бы полный обход на пятой минуте**. Очередь недоступна → 503 с явным текстом, а не тихий откат на синхронный обход.
- [x] 2026-07-29 **Двойная постановка задачи закрыта.** Лок берёт воркер, а не эндпоинт, поэтому между постановкой и подхватом два клика ставили **две** задачи — вторая начала бы повторный полный обход сразу после первого. `enqueue_run` отказывает и по локу, и по `is_running` в статусе (`already_running`, задача не ставится).
- [x] 2026-07-29 **Статус виден сразу.** При постановке пишется `parser:status:{slug}` с `is_running: true` и коротким TTL 15 мин — если воркер не поднят, статус протухает сам, а не висит «Выполняется» сутки. Прежний `products_found`/`last_run` больше не обнуляются на старте — при многочасовом обходе в колонке «Товаров» оставался бы 0 вместо прошлого результата.
- [x] 2026-07-29 **Админка: чекбокс «Полный обход каталога» + поле «Лимит позиций»** (`ParsersTable`), живая подпись эффекта, отказ на невалидный лимит, сообщение на `already_running`, подсказка «поднимите воркер» на 503. `useParsers` поллит каждые 5 с, пока хоть один парсер `is_running`.
- [x] 2026-07-29 **В `docker-compose.yml` добавлен сервис `worker`.** В прод-компоузе он был (`docker-compose.prod.yml:212`), в дев — нет: пока обход шёл внутри запроса, это не мешало, а теперь без воркера кнопка ставила бы задачу в никуда.
- [x] 2026-07-29 Проверено на живом стеке **без запуска воркера — то есть без единого обращения к магазинам**: `POST /run {"full_sync":true}` → 202 за 62 мс с `job_id`; в Redis `arq:queue`=1 и ключ `arq:job:<тот же id>`; `GET /api/admin/parsers` показывает `is_running: true` только у tgsm; `arq src.worker.WorkerSettings` рапортует `Starting worker for 12 functions: run_parser, …`. Тестовые задачи из Redis удалены. Тесты: **990 passed** (+15 к 975), ruff clean, фронт `tsc`+`build` 18/18+`lint`+`prettier` чисто.

- [ ] **Не сделано:** боевой прогон `sync_catalog` по реальным магазинам — это те самые 3–4 часа обхода на магазин, нужно отдельное согласие владельца и лучше ночное окно. Теперь для первой пробы есть дешёвый путь: запуск с лимитом 25–100 позиций из админки.

**⚠️ Смежная находка (не чинилось, вне блока).** `docker-compose.prod.yml:296` монтирует `./deploy/nginx/bscout.conf` как шаблон, но **этого файла в репозитории нет** — в `deploy/nginx/` лежит только `nginx.conf`. Прод-nginx с текущим compose не поднимется; там же должен жить `proxy_read_timeout`. Относится к Блоку 15.

## БЛОК 03 — Покрытие тестами (pytest: unit + integration, TDD)

**Статус:** [~] 2026-07-25 (бэкенд закрыт) · **17 → 975 тестов**, прогон 2026-07-28 — **975 зелёных** (падавший `test_every_device_has_at_least_two_aliases` починен: 14 устройств досева получили вторые алиасы по конвенции «кириллическая транслитерация бренда»). Осталась фронт-фаза (Vitest) — **`node_modules` теперь на месте, отговорки больше нет**, тест-раннер просто не заводили.

**Сделано TDD-ом по ходу блоков 02/05/06:** unit — `parse_price`/`normalize_name`/`compare_products`, `safe_request` (retry через httpx.MockTransport), `ParseResult`/`ParserManager`. Integration — `store_offers` uniq+CHECK+opt-цена, catalog seed идемпотентность, `ParserService` upsert+история+run_one, fuzzy `%`/`similarity` + gating, `PaymentService`. Починен баг тест-инфры (conftest: импорт моделей до create_all + extensions).

**Добор (5 агентов параллельно, каждый на своей БД `bscout_test_1..5`, +111 тестов):**
- `tests/unit/test_rate_limit.py` (20) — окно/границы/изоляция по IP/сброс, Retry-After, свип, e2e через ASGI.
- `tests/integration/test_auth_service.py` (18) — хэш в БД, uniq email (регистронезависимый), authenticate_user (успех/пароль/нет юзера).
- `tests/integration/test_search_history_and_cache.py` (17) — record/get_by_user (порядок, лимит, JSONB), `RedisCache` TTL/round-trip/инвалидация (ключи с уникальным префиксом).
- `tests/integration/test_deps_guards.py` (20) — `get_current_user` (нет/битый/refresh-токен/неактивный), `get_current_admin` 403/200, `require_active_subscription` (нет/активная/истёкшая).
- `tests/integration/test_api_contracts.py` (28) — health, register/login/me, 409 дубль, 422 невалидное тело, 401 неверные креды, пустой каталог, 404, admin 403, публичные эндпоинты.
- `tests/integration/test_regressions.py` (7, мои) — на исправленные баги (см. ниже).

**Фронт-фаза (не сделана):** Vitest + Testing Library + MSW — требует `npm install` (нет сети).

**Проблема.** У проекта нет тестового набора (см. `CLAUDE.md`), поэтому вся регрессия ловится руками через `MANUAL_VERIFICATION.md`. Нужен базовый каркас автотестов и дисциплина TDD для нового кода, чтобы бизнес-логику (auth/JWT, цены/подписки, гейтинг, нормализация парсеров, кэш) можно было проверять быстро и повторяемо.

**Стандарт, которого держимся (test pyramid, 2025).** Широкое основание быстрых **unit**-тестов чистой логики → умеренный слой **integration** (сервис + реальная БД/Redis) → тонкая верхушка **API/e2e**. Избегаем «ice-cream cone» (перекос в медленные сквозные тесты). Тестируем **поведение и контракты**, а не реализацию.

**Что покрываем (высокая отдача):**
- Auth: hash/verify пароля, JWT round-trip/тип/подпись/срок, `get_current_user`/`get_current_admin` (401/403).
- Деньги и доступ: таблица цен планов, `create_subscription` (деактивация прежней, срок trial=10/paid=30 дн), `require_active_subscription` (403 без подписки) — прямая связка с `MANUAL_VERIFICATION` «показанная цена == списанная», «гейтинг по тарифу».
- Парсеры: `normalize_name`, `parse_price` («4 500 ₽» → Decimal), `ParserManager`, upsert по `(store_id, external_id)` + запись `PriceHistory` при смене цены (когда появятся, Блоки 06–07).
- Инфраструктура: `RateLimitMiddleware` (окно/429), `RedisCache` (TTL, сериализация, отсутствие устаревшей цены после инвалидации).
- API-контракты ключевых эндпоинтов auth/users/products/search (коды, форма JSON).

**Что НЕ покрываем (низкая отдача / чужая ответственность):** тривиальные геттеры и `__repr__`; сериализацию самого Pydantic/SQLAlchemy; сторонние библиотеки (passlib, jose, FastAPI-роутинг как таковой); автогенерённые Alembic-ревизии; чистую вёрстку без логики; реальные сетевые скрейпы магазинов (нестабильны — остаются в `MANUAL_VERIFICATION`).

**Границы.** Блок не меняет прод-код (кроме `requirements-dev.txt`); тесты не коммитятся в общую БД (отдельная `bscout_test`). Пересечений с Блоками 01/02/06–08 нет — тесты добавляются поверх их результатов по мере готовности.

### Фаза 0 — Каркас и разделение слоёв *(готово)*
- [x] 2026-07-25 Инструментарий: `pytest`, `pytest-asyncio`, `pytest-cov` в `backend/requirements-dev.txt`; `backend/pytest.ini` (`asyncio_mode=auto`, маркеры `unit`/`integration`, `--strict-markers`).
- [x] 2026-07-25 Структура с изоляцией нагрузки: `backend/tests/{unit,integration}/` — `unit` без внешних сервисов (всегда зелёные и быстрые), `integration` требует Postgres/Redis. Запуск раздельно: `pytest tests/unit` vs `pytest tests/integration`.
- [x] 2026-07-25 `tests/integration/conftest.py`: движок на отдельную `bscout_test` (или `TEST_DATABASE_URL`), create/drop таблиц, фикстуры `db_session` и `client` (ASGITransport + override `get_db`); **graceful skip**, если БД недоступна.
- [x] 2026-07-25 Seed-тесты (TDD-каркас, 17 зелёных): unit — пароли, JWT, `ParserManager`, `config`-URL, цены планов; integration — register→login→tokens, `/api/users/me` без токена → 401/403.

### Фаза 1 — Unit: чистая бизнес-логика (`tests/unit/`) *(готово)*
- [x] 2026-07-28 Парсер-утилиты — `test_parser_utils.py` / `test_normalizer.py` / `test_extractor.py` + пять `test_parser_*` (tgsm/profi/liberti/divizion): `parse_price`/`normalize_name`/`compare_products`, извлечение полей.
- [x] 2026-07-28 `RateLimitMiddleware` — `test_rate_limit.py` (20) + `test_rate_limit_redis.py`: окно/границы/изоляция по IP/сброс, `Retry-After`, свип пустых бакетов, e2e через ASGI.
- [x] 2026-07-28 Валидация схем — покрыта в `test_api_contracts.py` (422 на невалидном теле, границы полей).

### Фаза 2 — Integration: сервисы + реальная БД (`tests/integration/`) *(готово 2026-07-28)*
- [x] 2026-07-28 `auth.service` — `test_auth_service.py` (18): `create_user` (хэш bcrypt в БД, null phone/company), uniq email регистронезависимый → IntegrityError, `authenticate_user` (успех/неверный пароль/пустой/нет юзера/чужой пароль), `get_user_by_email/id`.
- [x] 2026-07-28 `PaymentService` — `test_payment_service.py` (3: trial-срок, paid=30 дн, деактивация прежней) + сквозной `test_payment_flow.py` (24: idempotency, first-payment discount, confirm/webhook активирует ровно одну подписку, `cancel` держит доступ до `end_date`) + `test_trial.py` (10).
- [x] 2026-07-28 `SearchHistoryService` + `RedisCache` — `test_search_history_and_cache.py` (17): `record`/`get_by_user` (порядок desc, лимит, JSONB round-trip, изоляция по юзеру); кэш TTL/round-trip/`exists`/`expire`, «после инвалидации отдаётся свежая цена», эксклюзивный lock.
- [x] 2026-07-28 Зависимости доступа — `test_deps_guards.py` (20): `get_current_user` (нет заголовка/не-Bearer/битый/wrong-secret/refresh-как-access/просрочен/неизвестный/неактивный юзер), `get_current_admin` (403/200, неактивный admin → 401), `require_active_subscription` (нет/активная/истёкшая/деактивированная), `get_active_subscription`.
- Проверено полным прогоном 2026-07-28: **974 passed, 1 failed** (единственный красный — `test_catalog_seed::test_every_device_has_at_least_two_aliases`, `meizu-m3-max` 1 алиас вместо ≥2 — сид каталога, Блок 02, не относится к тестовому блоку; `seed.py` в незакоммиченной правке).
- [x] 2026-07-28 Починен ordering-зависимый флейк cache-тестов (синглтон `_cache` aioredis vs function-scoped loop): в `tests/integration/conftest.py` — `flush_test_redis` теперь обнуляет `_cache` НА ВХОДЕ каждого теста (свежий клиент в своём loop), `reset_redis_singleton` force-обнуляет даже при ошибке `aclose()` (мёртвый клиент не протекает в следующий тест). Прод `redis_cache.py` не трогали. Проверено: падавшее 7-файловое подмножество — 117/117 стабильно ×3.
- ⚠️ **Отдельный флейк — межпроцессная контеншн инфры, НЕ баг тестов (диагностировано 2026-07-28):** `test_recheck_sessions-oauth.py` плавающе даёт `ForeignKeyViolationError refresh_tokens→users` (2–4 падения/прогон) — но ТОЛЬКО когда одновременно идут два pytest-процесса на общей тест-БД. Доказано: тесты последовательны (нет `gather`); одиночный тест — 5/5 passed; на общей `bscout_test` при живом втором pytest — 2/4/3 падений; на выделенной `bscout_test_rc` — 0 падений. Остаток — от `flush_test_redis` → `flushdb()`, стирающего весь Redis db0 (общий для процессов). В одно-процессном CI файл зелёный.
  - **Фикс (по решению — после завершения параллельной redis-задачи, чтобы не конфликтовать по `conftest.py` и верифицировать на стабильном baseline):** per-process изоляция инфры — своя Postgres-БД на прогон (авто-имя по PID / `TEST_DATABASE_URL`), свой Redis-db-индекс на прогон (новое поле `redis_db` в `config.py`, backward-compatible, default 0), убрать глобальный `flushdb()` в пользу неймспейс-очистки. Делает конкурентные прогоны безопасными.

### Фаза 3 — API/e2e (тонкий слой, `tests/integration/`) *(готово)*
- [x] 2026-07-28 Auth-flow расширен — `test_token_lifecycle.py`, `test_oauth.py`, `test_password_reset.py`, `test_email_verification.py`: refresh-ротация, logout/повторный refresh, OAuth, сброс/подтверждение.
- [x] 2026-07-28 Products/search — `test_gating.py` (fuzzy только на advanced, гейтинг по тарифу) + `test_fuzzy_search.py`, `test_api_contracts.py` (пустой каталог), `test_search_history_and_cache.py` (запись истории).
- [x] 2026-07-28 Admin — `test_admin_bootstrap.py`, `test_deps_guards.py`: доступ только `is_admin` (403 обычному).
- [x] 2026-07-28 Негативные контракты — `test_api_contracts.py`: 422 на невалидном теле, 404 на отсутствующих, 409 на дубле.

### Фаза 4 — Фронтенд (по готовности Блока 01)
- [~] 2026-07-29 Каркас Vitest написан (запуск/верификация ждут `npm install` — тест-devDeps нет в `node_modules`, а `npm install` заблокирован системой прав): `vitest.config.ts` (alias `@`, plugin-react, coverage v8), `vitest.workspace.ts` (**разделение проектов: `unit`→node, `component`→jsdom** — «не грузить всё подряд»), `vitest.setup.ts` (jest-dom + cleanup). Скрипты в `package.json`: `test`/`test:unit`/`test:component`/`test:watch`/`test:coverage`; devDeps прописаны. Тест-файлы исключены из `tsconfig` Next, чтобы `next build` не падал.
- [x] 2026-07-29 Unit (node): `models/auth/schema.unit.test.ts` (Zod: валид/пароль<6/невалидный email/optional; **snake_case-контракт токенов** — camelCase отвергается), `shared/lib/format.unit.test.ts` (planLabel/price, formatDate, initials), `shared/lib/utils.unit.test.ts` (`cn` + twMerge-конфликты), `shared/config/store.unit.test.ts` (Effector через `fork`/`allSettled`, изоляция scope).
- [x] 2026-07-29 Проводка окружений (smoke): `src/test/harness.smoke.test.tsx` (render + jsdom + jest-dom), `src/test/msw/{handlers,server}.ts` + `src/test/msw.smoke.unit.test.ts` (перехват fetch через MSW).
- [ ] Компонентные формы login/register (submit, обработка 401) — **ждут Блока 01**: формы сейчас статичные (`<form>` без `onSubmit`), тестировать сабмит нечего. MSW-каркас готов для хуков React Query.
- ⚠️ Всё во Фазе 4 написано, но **не прогонялось** (нет тест-зависимостей). Верификация: `cd frontend && npm install && npm run test`.

### Фаза 5 — Покрытие и CI-гейт
- [!] `pytest --cov` **локально не запустить**: `pytest-cov` есть в `requirements-dev.txt`, но не установлен в venv, а `pip install` заблокирован (нет сети). В CI ставится и считается. Порог осознанно НЕ фиксировали — сначала нужна реальная цифра с CI.
- [x] Команды запуска задокументированы в корневом `CLAUDE.md` (заменено устаревшее «нет тестов»): `pytest tests/unit` (без внешних сервисов) / полный `pytest` (нужны Postgres+Redis + БД `bscout_test`) / `TEST_DATABASE_URL` для изоляции.
- [x] CI-workflow `.github/workflows/ci.yml`: сервисы postgres:16 + redis:7 с healthcheck, python 3.11, **`alembic upgrade head` с нуля** (этот шаг поймал бы HIGH-баг миграции), `pytest tests/unit`, затем полный прогон с `--cov`. Job `lint` (ruff check + format --check) — **блокирующая**: легаси-долг вычищен, бэкенд ruff-clean целиком (118 файлов, 0 замечаний).

### Проверка/DoD
- [x] `pytest tests/unit` — зелено без внешних сервисов; полный `pytest` — **974 passed / 1 fail** (2026-07-28) при поднятых Postgres/Redis; единственный красный — сид каталога (Блок 02), не тестовый долг.
- [x] Новый код добавлялся по TDD; каждый новый сервис/утилита пришли с тестом.
- [x] Неавтоматизируемое — в `MANUAL_VERIFICATION.md`.

### 🐞 Найдено тестами и ИСПРАВЛЕНО (главная ценность блока)
Тесты писались против реального поведения, поэтому вскрыли 6 настоящих багов прод-кода:
- **CRITICAL** `rate_limit.py` — троттлинг отдавал **500 вместо 429**: `HTTPException`, брошенный внутри `BaseHTTPMiddleware.dispatch`, не конвертируется (middleware стоит выше `ExceptionMiddleware`). Подтверждено эмпирически 3 независимыми агентами. Исправлено: возвращаем `JSONResponse(429)` + заголовок `Retry-After`. Заодно: убран `defaultdict` и добавлен периодический свип пустых бакетов (был вектор неограниченного роста памяти по IP).
- **HIGH** `user.py` — email регистрозависим: `Ivan@x.com` и `ivan@x.com` = разные аккаунты, вход не работает. Исправлено: колонка → `CITEXT` (миграция `c4115bee8c5e`, round-trip проверен), uniq теперь регистронезависимый.
- **HIGH** `search_history_service.record()` **не вызывался нигде** → `/api/search/history` всегда пустой, хотя кабинет (Блок 01) его показывает. Исправлено: запись в `search_products` при непустом `q` (фильтры + `results_count`).
- **HIGH** `users.py` — `GET/POST /me/subscription` смотрели только `is_active` без `end_date`: истёкшая подписка показывалась активной и **блокировала повторную оплату 409-м**. Исправлено: общий `get_active_subscription`.
- **MEDIUM** `payment_service.create_subscription` — `scalar_one_or_none()` без `limit(1)`: две активные подписки → `MultipleResultsFound` → 500. Исправлено: деактивируем все активные.
- **LOW** `authenticate_user` не проверял `is_active` — деактивированный пользователь получал валидные токены. Исправлено.

### 🐞 Второй раунд ревью (отдельный агент, вердикт «ship-with-nits») — ещё 6 правок
Ревьюер откатывал каждый фикс и проверял, что тесты падают → тесты честные, ничего не ослаблено. Дополнительно найдено и исправлено:
- **HIGH** rate-limit за прокси: все браузерные запросы приходят с IP Next.js-сервера → общий бакет 120 req/min на всех, и после фикса 429 это стало бы «весь апп затроттлен». Исправлено: `X-Forwarded-For` учитывается **только при явном** `RATE_LIMIT_TRUST_FORWARDED_FOR=true` (безопасный дефолт — не доверяем заголовку, иначе его можно подделать); лимиты вынесены в `Settings`.
- **MEDIUM** порядок middleware: `RateLimitMiddleware` оборачивал `CORSMiddleware` → у 429 не было CORS-заголовков (браузер видел непрозрачную CORS-ошибку), а OPTIONS-preflight'ы жгли лимит. Исправлено перестановкой; проверено эмпирически (429 с `Access-Control-Allow-Origin`, preflight → 200) + тест.
- **MEDIUM** `POST /users/me/subscription` создавал вторую строку с `is_active=True`, не гася протухшую → админ-счётчик врал, `cancel` гасил только новую. Исправлено: делегирование в `PaymentService.create_subscription` (гасит все активные).
- **MEDIUM** фикс `PaymentService` (несколько активных) был **без теста** — добавлен.
- **LOW/MEDIUM** история поиска писалась на **каждой странице** пагинации (3 страницы → 3 одинаковых записи). Исправлено: только `page == 1` + тест.
- **LOW** `max_requests <= 0` → `IndexError` (500 вместо блокировки) — добавлен guard + тест.
- **NIT→сделано** Redis-пул не закрывался вообще (ни в тестах, ни в проде): добавлены `close_redis()` + FastAPI `lifespan`; conftest теперь зовёт публичный API вместо приватного `_cache`.
- **NIT→сделано** `POST /auth/refresh` не проверял `is_active` — деактивированный мог 30 дней обновлять токены. Исправлено + тест.
- **LOW→сделано** индекс `(user_id, created_at DESC)` для истории поиска (миграция `91a61aee668d`; из автогенерации вручную убраны попытки снести GIN-trgm/partial индексы, которые autogenerate не видит).

**Не исправлено осознанно (нужно решение — см. вопросы):** `RedisCache` приводит `Decimal`→строку (`json.dumps(default=str)`); счётчики rate-limit живут в памяти процесса (при N воркерах лимит умножается; Redis дал бы общий счётчик); bcrypt молча обрезает пароль на 72 байтах, хотя схема разрешает 128; `client`-фикстура в тестах не коммитит (проверяет видимость во flush, не durability — ревьюер отдельно подтвердил, что коммит в проде работает).

**Известные заметки среды:**
- Postgres на хосте — порт **5434**; тест-БД `bscout_test` создаётся отдельно (`CREATE DATABASE bscout_test`), прод-данные не трогаются.
- Для параллельного прогона (несколько агентов/процессов одновременно) заведены изолированные БД `bscout_test_1..5`; выбираются через `TEST_DATABASE_URL=postgresql+asyncpg://bscout:bscout@localhost:5434/bscout_test_N`. Redis общий — в тестах ключи обязаны быть с уникальным префиксом.
- Реальная схema auth-эндпоинтов — **snake_case** (`full_name`, `access_token`), несмотря на заявленный в `CLAUDE.md` camelCase; у `RegisterRequest`/`TokenResponse` нет alias-конфига. Тесты идут по факту; выравнивание конвенции — вне этого блока.
- `pytest-asyncio`: async-фикстуры — function-scope (иначе ScopeMismatch с event loop).

## БЛОК 12 — Коммерческий контур: гейтинг, триал, лимиты, платежи

**Статус:** [~] · **P0**

**✅ РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-07-25): тарифы — строго по спецификации.** Триал **7 дней** (не 10, как было в коде и вёрстке). Все **5 магазинов на всех тарифах**. Мониторинг товаров **10 / 100 / 500**. Fuzzy, уведомления о цене и экспорт — только advanced. Единый источник правды — `src/modules/payment/service/plans.py`; цены/сроки/лимиты больше нигде не дублируются.

**Следствие для фронта:** тексты на `/tariffs` («2 / 15+ / 50+ поставщиков», «безлимитный поиск») и на `/register` («10 дней») **противоречат офёрте и подлежат замене** — страница тарифов должна брать данные из `GET /api/payment/plans`, а не хардкодить.

**Проблема.** Продукт раздаётся даром: `require_active_subscription` — **мёртвый код** (единственное вхождение — само определение), платные эндпоинты открыты любому залогиненному, `POST /payment/subscribe` выдаёт 30 дней advanced бесплатно и мгновенно, таблицы платежей нет, идемпотентности и вебхуков нет, ни один заявленный лимит тарифа не реализован. Триал обещан («10 дней» на фронте), но при регистрации не выдаётся, а оформить его можно бесконечно.

- [x] Модель + миграция `payments` (e2e438553863): сумма, валюта, статус с CHECK, provider_payment_id UNIQUE, идемпотентный ключ, confirmation_url, raw.
- [x] Разорвано: `subscribe` создаёт Payment(pending) и НЕ даёт доступ; подписка появляется только после подтверждения. Проверено вживую: subscribe → 201/pending, подписка → 404.
- [x] `require_active_subscription` навешен на роутер `/api/products` (все 6 эндпоинтов). Было мёртвым кодом. 403 со структурным detail `{code:'subscription_required'}`. Проверено: без подписки 403, с триалом 200.
- [x] Триал выдаётся при регистрации (7 дней по спецификации) в SAVEPOINT, чтобы падение не срывало регистрацию; `users.trial_used` защищает от повторной выдачи. Проверено вживую.
- [x] Матрица в `plans.py` + хелперы `get_current_plan/has_feature/require_feature/tracked_products_limit_for`. Enforcement лимита отслеживаемых товаров — в Блоке 13 (там появляется сама сущность).
- [ ] Истечение и продление подписок в arq-воркере (сейчас `is_active` никогда не гасится по `end_date`, а `auto_renew=True` без механизма продления).
- [x] Отмена снимает только `auto_renew`; доступ живёт до `end_date` — как и обещает UI.
- [x] `GET /api/payment/plans` — публичный, отдаёт цены/сроки/лимиты из `plans.py` (+ цена первого платежа со скидкой 20%).
- [~] Вебхук написан и **закрыт по умолчанию**: без `YOOKASSA_WEBHOOK_SECRET` отвечает 503 (проверено), с секретом — HMAC-подпись. ⚠️ Алгоритм подписи — заглушка, перед подключением мерчанта заменить на спецификацию ЮKassa. Временная замена вебхука — админский `POST /api/payment/{id}/confirm`. Фискализация 54-ФЗ и возвраты — не сделаны (нужны ключи мерчанта).
- [ ] Фронт: обработка 403 {code:'subscription_required'} — показывать CTA на тарифы вместо общей ошибки (перенесено из Блока 11).

## БЛОК 13 — Мониторинг товаров и уведомления о цене

**Статус:** [~] 2026-07-25 · **P0** · бэкенд закрыт; фронт вынесен в **Блок 20**, канал доставки уведомлений не выбран

**Итог.** Тарифная сетка перестала продавать несуществующее. Проверено вживую: счётчик 0/10 на триале (был выдуманный «42/100»), 11-й товар → **402** `{code:'tracking_limit_reached', used:10, limit:10}`; снижение цены 6070→5570 ₽ рождает уведомление, повторный прогон — ноль (дедупликация), рост цены не уведомляет. Тесты 451 → **512**.

**Проблема (найдено при сверке с ТЗ/`subscriptions.md`).** Все лимиты тарифов заданы через **«Мониторинг товаров» — 10 / 100 / 500 отслеживаемых позиций**, а главная фича Продвинутого — **уведомления о снижении цены**. В коде **нет ни одной сущности** watchlist/tracking/notification (grep по `backend/src` — пусто). То есть тарифы продают функциональность, которой не существует, а Базовый и Продвинутый функционально почти неразличимы (единственное отличие сейчас — fuzzy).

- [x] Модели `tracked_products` + `notifications` (миграции 1ea946afe7ef, 51e60c6b40f1). Добавлена `initial_price`: без неё «дельта с момента добавления» молча превратилась бы в «дельту с последней проверки», как только детектор начнёт двигать `last_seen_price` — дефект на стыке двух агентов, виден только после объединения.
- [x] `POST/GET/PATCH/DELETE /api/tracking` + `GET /api/tracking/usage`. Лимит 10/100/500 с 402 и структурным detail. Реактивация записи тоже проверяет лимит — иначе он обходился циклом деактивация/включение. Чужая запись → 404, не 403 (не раскрываем существование). `/usage` намеренно **не** под гейтингом: истёкшая подписка должна видеть счётчик и предложение продлить.
- [x] `AlertService.scan_for_drops` — `target_reached` приоритетнее `price_drop`; `last_seen_price` обновляется и при росте (иначе возврат к прежней цене засчитался бы как снижение); дедупликация по UNIQUE `dedup_key` с ловлей гонки на SAVEPOINT.
- [~] Уведомления создаются только на advanced (`has_feature('price_alerts')`); API `/api/notifications` + unread-count + пометка прочитанным. Канал доставки **подключаемый** (`Notifier` → InApp/Logging). ⚠️ Остаётся **решение владельца**: email или телеграм — после выбора нужна одна реализация интерфейса и доступы (SMTP или бот-токен). Отписка не сделана — привязана к выбору канала.
- [~] Фронт: `/tariffs` и `/subscription` переведены на `GET /api/payment/plans` (выдуманные «2/15+/50+ поставщиков» и «безлимит» убраны, `/register` больше не обещает 10 дней), добавлено честное состояние «платёж ожидает подтверждения». ⚠️ Кнопка «Отслеживать» в карточке/выдаче и раздел мониторинга в кабинете **не сделаны** — нужен фронтовый слой `models/tracking` поверх готового API. Проверка: захардкоженного «42/100» во фронте не оказалось — прежняя запись в плане была неточной.

## БЛОК 15 — Эксплуатация и деплой

**Статус:** [~] основное готово, боевого развёртывания не было · **P1**

**Проверено, а не заявлено:** `docker compose config` на прод-оверлее валиден (7 сервисов), образ бэкенда собирается (427 МБ), запускается от непривилегированного пользователя `app`, **отказывается стартовать с дефолтным JWT-секретом прямо в контейнере**, при `DEBUG=false` Swagger закрыт. Наружу опубликованы только 80/443 — Postgres и Redis больше не торчат на хост.

⚠️ **Три агента Блока 15 зависли, не завершив работу** (6 попыток каждый, 373k токенов). Двое успели сделать файлы, но не закоммитили — работа спасена из их worktree и проверена мной. Третий (наблюдаемость) не сделал ничего — написал сам.

**Проблема.** Развернуть нечего и небезопасно: у фронта **нет Dockerfile** (файл-заглушка 0 байт) и сервиса в compose; arq-воркера в compose нет, т.е. парсинг по расписанию не выполняется вовсе; нет reverse-proxy и TLS, а CORS прибит к `http://localhost:3000` — на реальном домене браузер получит отказ; пароль Postgres захардкожен, порты БД/Redis публикуются на хост; в бэкенде **ни одной строки логирования**, нет error-tracking и метрик; `/api/health` не проверяет зависимости; миграции в CMD контейнера — гонка при репликах; у растущих `offer_price_history`/`search_history` нет ретеншна и бэкапов.

- [x] `docker-compose.prod.yml`: секреты только из env (compose откажется стартовать без них), порты БД и Redis сняты с хоста, пароль Redis (**добавил поддержку `redis_password` в конфиг и воркер — приложение его не умело**), healthcheck у всех сервисов, отдельный одноразовый сервис `migrate`, и **сервис `worker`** — без него в проде не выполнялось ничего из расписания: ни обновление каталога, ни уведомления о ценах, ни истечение подписок.
- [x] `frontend/Dockerfile` (его не существовало) + сервис в compose. ⚠️ Сборка образа фронта не проверялась — только бэкенда.
- [x] Nginx: обратный прокси, TLS, редирект http→https, проброс `X-Forwarded-*`, ограничение размера тела запроса (аудит отмечал его отсутствие), gzip, security-заголовки. CORS больше не прибит к localhost — список источников задаётся настройкой.
- [x] Миграции убраны из CMD в отдельный сервис `migrate`, от которого зависят backend и worker: при нескольких репликах они дрались за блокировку.
- [x] JSON-логирование (раньше `basicConfig` не вызывался нигде — логов не было) с **фильтром секретов**: вычищаются Authorization, JWT, пароли, токены сброса и ссылки из писем. Request-id сквозной. `/api/health/live` и `/api/health/ready` с реальной проверкой Postgres и Redis — раньше оркестратор считал живым бэкенд с отвалившейся БД. 14 тестов. ⚠️ Sentry и метрики не сделаны.
- [x] Скрипты бэкапа и восстановления (восстановление требует явного подтверждения — оно затирает базу). Ретеншн: история поиска старше 90 дней удаляется, история цен старше 180 дней **прореживается до одной точки в день** и полностью удаляется после 365 — график в карточке товара при этом не ломается. Пакетами, еженедельной задачей в arq. 10 тестов.
- [x] 2026-07-28 **CI достроен.** Четыре джобы: бэкенд (миграции round-trip + проверка, что GIN-индексы его пережили, юнит-тесты, полный набор с покрытием, наполнение данными, порог точности матчинга), фронтенд (tsc + lint + build), стиль (ruff check + format), образы (сборка бэкенда и фронтенда + валидация прод-конфигурации compose). Весь набор гоняется с `DEBUG=false` — прод-дефолт проверяется, а не подразумевается.
- [x] 2026-07-28 Шаги проверены локально теми же командами и переменными: цепочка seed→import→match→precision даёт 0.32% при пороге 1.0% (код 0); round-trip миграций сохраняет 5 GIN-trgm индексов; `docker compose config` на прод-оверлее проходит.

### Открыто — мешает развернуть и тестировать *(заведено 2026-07-29)*
- [ ] **`deploy/nginx/bscout.conf` отсутствует в репозитории**, хотя `docker-compose.prod.yml:296` монтирует его как шаблон, — прод-nginx с текущим compose **не поднимется**. В `deploy/nginx/` лежит только `nginx.conf`, который сам его `include`-ит. Там же должен жить **`proxy_read_timeout`**: дефолтные 60 с рвут любой долгий админский запрос.
- [ ] **Образы отстают от кода.** Контейнер бэкенда собран из старого дерева (проверено 2026-07-29: нет маршрутов экспорта, `review-offers`, health-проб, асинхронного запуска парсера), исходники в контейнер не монтируются — «свежий код в контейнере» бывает только после `docker compose build`. Образ фронта не собирался **ни разу**. Нужно: пересборка обоих + шаг сборки образов в CI, иначе любое ручное тестирование идёт по устаревшему API.
- [ ] **В дев-компоузе теперь четыре сервиса, а не три** — `worker` добавлен 2026-07-29 (Блок 08); без него запуск парсера из админки уходит в никуда.
- [ ] Сид как шаг деплоя — документировать в CI и compose (перенесено из Блока 09).

## БЛОК 17 — Матчинг v2: покрытие устройств и качество на живых данных

**Статус:** [~] · **Заведён:** 2026-07-26 · **P1** · **Зависит от 02, 10** · Требует сети (живые замеры) · Фазы 2–5 закрыты; открыты Фаза 1 (бейзлайн-таблица) и полная петля `gaps`→автокандидаты

**Проблема (найдено живым скрейпом siriust 2026-07-26).** Блок 10 закрыл матчинг с «100% покрытием», но это на 308 **фикстурных** офферах. Первый живой замер (68 реальных заголовков siriust, read-only прогон через `extract()`): **auto 38.2%, candidate 0.0%, unmatched 61.8%**. Вскрыты две структурные проблемы (не баги реализации):
1. **Словарь устройств — узкое место.** Все 42 unmatched имеют `device_id=None` при верно извлечённом `part_type` (АКБ→part_type найден). Сид из 57 устройств покрывает в основном iPhone; Samsung/Xiaomi/Honor/Realme/Tecno и модели по MPN/кодовому имени (BG920ABE, G930F, J400) не опознаются. Auto-порог 0.8 = device(0.5)+part_type(0.3) → без device авто-матч невозможен по построению.
2. **«Мёртвая середина» скоринга — очередь модерации недостижима.** `_INCOMPLETE_CORE_FACTOR=0.5` при отсутствии device роняет итог до ≤0.25 (part_type-only = 0.15). Порог кандидата 0.5 не берётся → 0 кандидатов на 68 позиций. Предохранитель `match_candidates` из дизайна 02/10 на практике обходится: реальные позиции молча падают в unmatched вместо ревью.

Итог: суть продукта (сравнение цен) на живых данных работает лишь для ~⅓ позиций, а самообучающийся контур модерации не запускается.

**Границы / пересечения:**
- Доработка матчера Блока 10 (`normalizer.py`/`extractor.py`/`matching_service.py`) и справочников Блока 02 (`catalog`) — не переписывание.
- Не трогает API сравнения (Блок 10) и фронт (Блок 11) — контракты не меняются.
- Родственный баг парсеров (`stock_status="out_of_stock"` вне CHECK у `profi`/`tgsm`) уже исправлен на `"out"` при валидации — здесь только контекст, отдельной задачей не идёт.

### Фаза 1 — Бейзлайн по всем 5 магазинам
- [ ] Read-only живой прогон `extract()` по реальным заголовкам каждого магазина (siriust уже: 38% auto) → таблица auto/candidate/unmatched по магазину.
- [ ] Разложить unmatched по причине (нет device / part_type / quality) — понять, где именно течёт.
- [x] 2026-08-08 **Замер на живых данных: главная причина `unmatched` — не матчер, а непрофильный товар в выборке.** Прогоны по 50 позиций дали `auto`/`review`/`unmatched`: Profi с `section=zapchasti` **50/0/0**, Profi без фильтра **22/4/24**, Divizion с фильтром **41/3/0**, Liberti **13/32/5**, TGSM **0/16/34**. Прямое доказательство — один и тот же магазин и лимит: без фильтра 44% `auto`, с фильтром 100%. В `unmatched` попадали стойки для дрелей и гермомешки (Profi), гарнитуры и FM-модуляторы (TGSM), скотч и автодержатели (Liberti) — матчер помечает их верно. Отдельная течь у Liberti: 32 `review` — легаси-устройства, которых нет в словаре (`Motorola W220`, `Samsung Galaxy Mega 5.8`, `HTC Evo 3D`).

### Фаза 2 — Починка «мёртвой середины» *(готово 2026-07-26)*
- [x] 2026-07-26 Диагностика: «мёртвая середина» — не порог, а структура. Кандидат требует `product`, а `product` требует device+part_type (`MatchCandidate.product_id` NOT NULL, очередь модерации — INNER JOIN на Product). Значит «part_type есть, device нет» кандидатом стать не может в принципе.
- [x] 2026-07-26 Решение — новый статус `store_offers.match_status='review'`: оффер с распознанным part_type/device, но несобираемым каноном, уходит в видимый бэклог `review`, а не молча в `unmatched`. Маршрутизация по структуре (наличие device/part_type), экстрактор/веса не трогаются. Правки: модель+CHECK, миграция `b1c2d3e4f5a6`, `matching_service` (+счётчик `review` в `MatchStats`).
- [x] 2026-07-26 Асимметрия против ложных склеек сохранена: `auto` (≥0.8) по-прежнему только device+part_type; `review` не protected → перематчится в `auto`, когда словарь пополнится (Фаза 3).
- [x] 2026-07-26 Живой замер siriust (68 позиций): `unmatched` 61.8%→0%, `review` 0→61.8% (42 шт. — бэклог наполнился). Тесты: +1 (`test_known_part_unknown_device_routes_to_review`), весь сюит зелёный (702).
- [x] 2026-07-26 `review`-бэклог выставлен в API модерации: `GET /api/admin/review-offers` (пагинация, admin-only) + `ModerationService.list_review_offers` + схема `ReviewOfferListResponse`. Резолв — уже существующим `POST /offers/{id}/link` (работает на любой оффер). +2 теста (список только `review` + admin-only). Роут зарегистрирован, приложение импортируется.

### Фаза 3 — Расширение словаря устройств *(готово 2026-07-26)*
- [x] 2026-07-26 Data-driven gap-анализ живого siriust (251 позиция) выявил точный хвост: все старые iPhone (5S–XS Max), Samsung по MPN (J320F/A605FN/G930F…), старые Xiaomi/Honor/Realme/Infinix/Oppo/Tecno.
- [x] 2026-07-26 Пополнение сида `DEVICES` (+~65 устройств, бренды Infinix/Tecno) с реальными алиасами (MPN + маркетинговое имя). Сид идемпотентен (get_or_create по `model_key`/`alias`) — существующее не ломается; на dev-БД теперь 124 устройства / 375 алиасов.
- [x] 2026-07-26 Процесс пополнения оформлен first-class: CLI `python -m src.cli gaps` (+`MatchingService.device_gap_report`) репортит топ нераспознанных устройств из `review`-офферов — петля дообучения, а не разовый хардкод. +1 тест.
- [ ] Остаётся (полная петля): из `gaps`-репорта автогенерировать кандидатов-алиасов в очередь модерации с подтверждением — сейчас репорт есть, апрув-в-словарь ручной.

### Фаза 4 — Замер и приёмка (DoD)
- [x] 2026-07-26 Живой замер siriust до/после Фазы 3: общий сэмпл (68) **auto 38.2%→64.7%**, `unmatched`→0; «старый» сэмпл (251) **auto 17.5%→62.5%**. Остаток — обоснованный длинный хвост (частота 1–2: Redmi 3/4, Samsung A5 2016, Oppo R17), в `review`, не потерян.
- [x] 2026-07-26 Результаты (до/после) → `MANUAL_VERIFICATION.md`.
- [x] 2026-07-26 Замер по всем магазинам (live, read-only extract) + gaps-досев. **auto до→после досева:** siriust 64.7% · **divizion 59.6%→82.9%** · **liberti 41.3%→79.3%** · **tgsm 33.3%→50.0%**; `unmatched`≈0 везде (всё нераспознанное → `review`). Досев data-driven по `gaps`-хвостам (+~48 старых Honor/Redmi/Realme/Oppo/Samsung; словарь 389 устройств). Greenspark — вживую нельзя (нет парсера, бот-защита). tgsm упирается в осознанно не-досеянный хвост (Alcatel/кнопочные/планшеты/радиодетали). Петля `gaps`→досев подтверждена как работающий рычаг.

### Фаза 5 — Точность, а не только полнота *(2026-07-28)*

**Слепое пятно предыдущих фаз.** Фазы 1–4 мерили `auto/review/unmatched` — то есть **полноту**. Никто не проверял, **верны ли** сопоставления, попавшие в `auto`. Замер точности на 395 живых названиях Дивизиона вскрыл: **6% авто-сопоставлений вели на ДРУГОЙ телефон** — `Дисплей для Tecno Spark 40 Pro` уходил на `Tecno Spark 40`.

Для агрегатора цен это опаснее пропуска: пропуск сокращает выбор, а неверное сопоставление показывает **цену не того товара** — пользователь едет в магазин и обнаруживает подмену. Метрика `auto%` при этом выглядела здоровой.

- [x] 2026-07-28 Причина: вариантов Pro/Plus/Ultra не было в словаре, извлекатель брал базовую модель. Досеяны варианты, встреченные вживую (Tecno Spark 8/10/40 Pro, Pop 4 Pro, Camon 30 Pro, Pova 6/6 Pro, Infinix Note 11/12/30/50 Pro, Hot 40i, Smart 8, Huawei Nova 13 Pro, POCO M7/F6/F3 Pro, Xiaomi 13T/14T Pro, Redmi Note 12 Pro+).
- [x] 2026-07-28 **Неверных сопоставлений 6% → 2%**; распознавание устройства 82% → 85%, устройство+тип детали 81% → 84% (395 названий Дивизиона).
- [x] 2026-07-28 Убраны доменные неоднозначности: `note 11 pro` и `note 12 pro` есть и у Xiaomi Redmi, и у Infinix. Для сравнения цен неоднозначный алиас **хуже отсутствующего** — он даёт уверенно неверный ответ вместо честного пропуска. Оставлены только уточнённые брендом.
- [x] 2026-07-28 Список `DEVICES` переписан целиком: 389 устройств, у каждого **минимум два алиаса**, коллизий ноль. Из БД удалены три осиротевших дубликата Huawei без алиасов.
- [x] 2026-07-28 **Метрика точности встроена в петлю.** `MatchingService.precision_report` / `precision_stats` + CLI `python -m src.cli precision`: находит авто-сопоставления, где в названии есть суффикс модели (Pro/Plus/Ultra/Max/FE/Lite/NFC), которого нет у сопоставленного устройства. Флаг `--max-share` возвращает код выхода 1 при превышении порога — то есть отчёт годится для CI, а не только для чтения глазами. 6 тестов.
- [x] 2026-07-28 Отчёт сразу окупился на живой базе: нашёл 5 подозрительных (1,62%) — `POCO X3 NFC` и `X3 Pro`, привязанные к обычному `POCO X3`. После `match --all` осталась **одна** позиция (0,32%), и это настоящая мультимодельность (`Poco X3 NFC / X3 Pro` — одно предложение на два телефона), то есть ровно область Блока 18. Отчёт сам изолировал причину.
- [x] 2026-07-28 `precision --max-share 1.0` подключён к CI отдельным шагом: деградация точности роняет сборку. Перед проверкой CI наполняет базу (`seed` → `import` → `match --all`) — на пустой базе метрика была бы бессмысленной (0 авто-матчей = 0% подозрительных). Для этого добавлена CLI-команда `import` (раньше импорт был только HTTP-эндпоинтом).

**⚠️ Известное ограничение ранжирования (не чинилось осознанно).** Извлекатель предпочитает **раннее** совпадение более специфичному: у `Galaxy S23` есть алиас «самсунг s23», и в названии `Samsung S23 Plus` он срабатывает раньше, чем `s23 plus` → выбирается S23. Ключ ранжирования `(size, contiguous, -positions[0], len(surface))` ставит позицию выше специфичности. Не трогалось: от этой логики зависят 967 тестов, а в живых данных магазины почти всегда пишут заводской код (`S916/ S23 Plus`), который снимает неоднозначность. Чинить — только вместе с замером точности до/после.

**Известные риски/заметки:**
- «100%» из Блока 10 — метрика на фикстурах; не путать с живым качеством.
- Словарь — бесконечный хвост; цель не 100%, а высокий auto + рабочая очередь на остальное (осознанно не гнаться за полным покрытием).
- Замеры требуют сети и вежливого рейта (siriust уже показывал чувствительность к частоте).

## БЛОК 18 — Мультимодельные предложения: одно предложение — несколько товаров

**Статус:** [ ] · **Заведён:** 2026-07-28 · **P2** · **Зависит от 02, 10, 17** · **Меняет ядро данных — только отдельной сессией**

**Проблема.** Одна карточка магазина часто относится к нескольким устройствам сразу:
`Аккумулятор (АКБ) для Xiaomi Redmi Note 12 5G/ Poco X5 5G`, `Дисплей для Xiaomi Note 13 Pro+ 5G/ Note 14 Pro 5G/ Poco X7 5G` (три устройства). Модель данных допускает **ровно одну** связь: `store_offers.product_id` — один внешний ключ. Значит предложение привязывается к первому распознанному устройству, а для остальных **молча исчезает из сравнения**: пользователь, ищущий Poco X5, не увидит эту батарею и может не найти самую дешёвую позицию.

**Замер (Дивизион, 395 живых названий, 2026-07-28).** Слэш встречается в **31%** названий, но настоящих мультимодельных — **около 5%**. Остальное — шаблон «заводской код/ рыночное имя» одного и того же устройства (`Samsung A075/ A07`, `Samsung S916/ S23 Plus`), который **не требует** изменения модели. Настоящие случаи сосредоточены в **аккумуляторах**: одна батарея физически подходит нескольким моделям.

**Почему отложено.** Ради 5% менять ядро преждевременно, пока точность важнее охвата (см. Блок 17, Фаза 5: неверные сопоставления 6%→2%). Задача станет обязательной, когда аккумуляторы пойдут в ассортимент всерьёз — там совместимость с несколькими моделями норма, а не исключение.

### Что делать

- [ ] **Схема:** таблица связей `offer_products(offer_id, product_id, is_primary, confidence)` с `uniq(offer_id, product_id)`. `store_offers.product_id` **сохранить** как первичную связь — от неё зависят агрегаты, трекинг и 967 тестов; ломать контракт нельзя.
- [ ] **Извлечение:** `extractor.extract()` возвращает одно устройство. Нужен режим, возвращающий **список** устройств. ⚠️ Обязательно отличать `код/ имя` одного устройства от двух разных: наивное разбиение по слэшу даст ложные связи (проверено — детектор по слэшу завышает в 6 раз).
- [ ] **Матчинг:** `MatchingService` создаёт связь на каждое распознанное устройство, первичной помечает наиболее уверенную.
- [ ] **Агрегаты сравнения:** `ComparisonService` считает предложения через `store_offers.product_id`. Перевести на таблицу связей, иначе мультимодельные предложения не появятся в выдаче — ради чего всё и делается. ⚠️ Это самая рискованная часть: агрегаты завязаны на индексы и покрыты тестами.
- [ ] **Отслеживание и уведомления:** `TrackedProduct` и `AlertService` тоже ходят через `product_id` — проверить, что снижение цены на мультимодельном предложении доходит до всех подписанных товаров.
- [ ] **Экспорт:** выгрузка каталога считает `offers_count`/`stores_count` — сверить, что цифры не задвоятся.
- [ ] **Замер до/после** на живых данных: доля предложений, доступных из сравнения; отдельно проверить, что **точность не упала** (Блок 17, Фаза 5).

### Риски

- **Задвоение в агрегатах.** Если предложение связано с тремя товарами, наивный `JOIN` посчитает его трижды в «числе предложений» и может исказить «магазинов». Нужны явные `DISTINCT` и тесты именно на это.
- **Двойной учёт в истории цен.** `offer_price_history` привязана к офферу, а не к товару, — при агрегации по нескольким товарам одна и та же точка попадёт в несколько графиков. Это может быть правильно, но должно быть осознанным решением, а не побочным эффектом.
- **Ложные связи хуже пропусков.** Если разбиение ошибётся, батарея от одной модели окажется в сравнении другой — то же зло, что чинили в Блоке 17, Фаза 5.

### Приёмка

Мультимодельное предложение видно в сравнении **всех** связанных товаров; `offers_count` и `stores_count` не задвоены; точность сопоставлений не хуже, чем до изменения; полный набор тестов зелёный.

## БЛОК 19 — Жизненный цикл аккаунта, соцвход и аудит безопасности

**Статус:** [~] · **Заведён:** 2026-07-28 задним числом · **Работа выполнена 2026-07-26…27** (коммиты `eae597c`…`c45204b`) · **Ветка:** `feature/backend-core`

**Почему блок заведён задним числом.** Тринадцать коммитов закрыли последний открытый пункт Блока 14 («смена пароля, восстановление, подтверждение email — не сделано») и добавили соцвход, которого в плане не было вовсе. Ни один блок этого не учитывал: при сверке 2026-07-28 план утверждал, что восстановления пароля нет, хотя оно работало вторые сутки. Блок фиксирует сделанное и, главное, **что осталось незакрытым** — иначе это выпадает из плана целиком.

### Сделано (бэкенд)
- [x] 2026-07-26 **Почтовый слой** `src/modules/mail`: `Mailer` + `ConsoleMailer`/`SmtpMailer` (smtplib через `asyncio.to_thread`, SSL 465 — `aiosmtplib` не ставили), выбор по `settings.mail_backend`. При `mail_backend=smtp` без host/user/password — явная `MailNotConfiguredError` со списком недостающих ключей, а не тихое проглатывание. Ссылки строятся **только** из `settings.frontend_base_url`: подделка заголовка `Host` не даёт фишинговую ссылку от нашего имени. Токены и ссылки в лог не пишутся.
- [x] 2026-07-26 **Восстановление и смена пароля:** `POST /api/auth/password-reset/request` (всегда 202 с одинаковым телом + сопоставимая работа для неизвестного адреса — форма не превращается в проверялку аккаунтов), `/password-reset/confirm` (sha256, одноразовость, TTL 1 ч, единый 400 на все причины отказа), `/password/change` (обязательна проверка текущего пароля). В БД — только `sha256(token)`. После сброса и смены отзываются **все** refresh-токены, текущему клиенту сразу выдаётся новая пара.
- [x] 2026-07-26 **Подтверждение email:** `/email/confirm` (одноразовый токен, TTL 24 ч, sha256 в БД), `/email/resend` (квота 3 письма в час, fail-closed при недоступности Redis), зависимость `require_verified_email`. Письмо уходит при регистрации; сбой отправки не ломает регистрацию.
- [x] 2026-07-26 **Вход через ВКонтакте** (`/vk/authorize`, `/vk/callback`, `/vk/link`) — существующий email **не** привязывается автоматически (защита от захвата аккаунта); последний способ входа отвязать нельзя. **Привязка Телеграма** (`/telegram/prepare`, `/telegram/link`) — HMAC-SHA256 с секретом `sha256(bot_token)`, constant-time сравнение, обязательный nonce, привязанный к пользователю, окно `auth_date` 60 с, набор параметров виджета одноразовый (Redis SETNX).
- [x] 2026-07-27 **Denylist access-токенов** в Redis (`auth:invalidate:{user_id}`): при отзыве всех сессий `get_current_user` сверяет `iat`. Логаут теперь гасит и access-токен — раньше украденный жил ещё 15 минут после «выхода».

### 🐞 Найдено атакующими агентами и исправлено (подтверждено запуском)
- **HIGH — захват аккаунта через ВК-state.** State хранил просто `"1"`: ни назначения, ни владельца, один пул на вход и привязку. Атакующий брал state анонимно, заманивал залогиненную жертву на `callback` со своим `code+state` — и его ВК привязывался к аккаунту жертвы, дальше вход под ней. Теперь state хранит `{purpose, user_id}`, `/vk/authorize` имеет режимы login/link, привязка требует state, выпущенный самим пользователем.
- **HIGH — бесплатная подписка.** `POST /api/users/me/subscription` выдавал активный тариф на 30 дней без оплаты — недочищенный хвост Блока 12 (там закрыли `/payment/subscribe`, а второй путь остался). Теперь 402.
- **HIGH — гейтинг подтверждённой почты не был навешен ни на один эндпоинт:** `require_verified_email` существовал, но не использовался. Навешен на оформление подписки.
- **Email-бомбинг и гашение чужой ссылки:** строгий лимит распространён на все три парольных эндпоинта; окно из 3 одновременно валидных reset-токенов + cooldown 60 с — атакующий больше не гасит ссылку из письма жертвы; отправка вынесена в arq-задачу (убирает тайминг-оракул и зависание на недоступном SMTP); атомарный `UPDATE ... WHERE used_at IS NULL RETURNING` вместо чтения без блокировки.
- **⚠️ Едва не ослабили проверку сами:** массовая правка тестов добавила nonce в тест, который как раз доказывал работу **без** nonce. Восстановлено вручную. Урок: механическая правка тестов под новый контракт умеет незаметно стирать сам предмет проверки.

### Аудит безопасности (3 независимых агента, `docs/SECURITY_AUDIT.md`)
- [x] 2026-07-27 **Угол «авторизация, IDOR, привилегии, гейтинг» — 0 находок** на 62 маршрутах, проверенных живыми запросами.
- [x] 2026-07-27 Исправлены обе MEDIUM: детект кражи refresh-токена не гасил access-токены (защита работала наполовину именно там, где нужнее); тайминг-оракул на `/auth/login` (223 мс против 6 мс) — добавлен хэш-пустышка, тест требует разницы менее чем втрое. Плюс `debug=True` по умолчанию → `False`, обязательный `exp` в `decode_token`, верхняя граница `page`.

### Осталось (требует решений владельца или отдельной работы)
- [ ] **Фронт не подключён ни к одной из этих функций.** На `/login` «Забыли пароль?» — это `<a href="#">`; страниц сброса пароля, подтверждения email и кнопок ВК/Телеграм нет. Бэкенд готов и не используется — см. Блок 20, там же имеет смысл делать.
- [ ] **`python-jose` заброшена и имеет известные CVE** — в текущей конфигурации не эксплуатируется (алгоритм фиксирован), заменить на `pyjwt`.
- [ ] **Denylist access-токенов живёт только в Redis** — перезапуск воскрешает отозванные токены на остаток жизни (до 15 мин). Надёжнее колонка `users.tokens_valid_after`.
- [ ] **Энумерация аккаунтов при регистрации** по коду 409 — полное устранение требует смены контракта на «проверьте почту».
- [ ] **Телеграм-виджет:** перехваченный набор внутри окна 60 с применим другим аккаунтом — свойство самого виджета. Правильное решение — привязка через deep-link бота, которая заодно даёт `chat_id`, необходимый для **отправки уведомлений** (см. Блок 13: канал доставки так и не выбран).
- [ ] **Удаление аккаунта** — не сделано (заявлено проблемой в Блоке 14, ни одним блоком не закрыто).

<!-- BLOCKS:END -->

## История обновлений

- 2026-07-29 — **Парсер доведён до безопасного рабочего контура.** Четыре доступных магазина прошли live-smoke 3/3; починены discovery Profi и фильтрация sitemap Divizion. Добавлены валидация цены/обязательных полей, безопасная деактивация stale-офферов, явная статистика refresh, opt-in ночного full sync и корректный healthcheck worker. GreenSpark остаётся внешним блокером: бот-защита и отсутствие подтверждённой точки в Ставрополе; обход защиты не реализуется. Проверено: 1007 backend-тестов, ruff, frontend build/lint.
- 2026-07-29 — **Блок 08: запуск парсера стал управляемым и асинхронным.** Отправная точка — анализ парсера: реализовано ~80% кода (каркас, 4 из 5 магазинов, планировщик, админка; ГринСпарк закрыт бот-защитой), на реальном HTML магазинов 100% извлечённых записей проходят валидацию полей, но у **28% неизвестно наличие** (Либерти: 33 из 37 позиций раздела) и до сравнения цен доходит ~75% позиций. Попутно вскрылось четыре дефекта, каждый чинился по мере обнаружения: (1) **`full_sync` был мёртвым параметром** с Блока 06 — `run_one` его игнорировал, поэтому «Запустить» из админки всегда означало полный обход на 3–4 часа; (2) **лок `parser:lock` жил час при обходе в 3–4 часа** — истекал посреди прогона и пускал второй поверх первого; (3) **обход шёл внутри HTTP-запроса** — на 500 позициях (8–10 мин) он гарантированно ловил `proxy_read_timeout` nginx и показывал админу 504 при успешно отработавшем парсере; (4) после перевода в очередь — **двойная постановка задачи** между enqueue и подхватом воркером. Итог: `POST /run` отдаёт 202 за 62 мс, задача уходит в arq (`func(..., timeout=6ч)` — дефолтные 300 с убили бы полный обход), в админке появились чекбокс полного обхода и поле лимита с поллингом статуса, в дев-`docker-compose.yml` добавлен отсутствовавший сервис `worker`. Проверено на живом стеке **без запуска воркера — ни одного обращения к магазинам**. Тесты 975 → **990**. Смежно найдено: `docker-compose.prod.yml` монтирует несуществующий `deploy/nginx/bscout.conf` — прод-nginx не поднимется (Блок 15).
- 2026-07-29 — **Блок 20 закрыт: мониторинг товаров стал доступен пользователю.** Слайсы `models/tracking` и `models/notification`, виджет `TrackButton` (карточка товара + карточка выдачи, общий кэш на страницу), разделы `/account/tracking` и `/account/notifications`, счётчик непрочитанных в шапке, разбор 402/403 по смыслу с CTA на тарифы. Попутно подключена кнопка выгрузки `tracking.csv` — она существовала с Блока 16 и не была выведена в UI. Проверено вживую: лимит 10/10 → 402 с честным текстом; снижение цены 3130→2330 ₽ → уведомление и бейдж «1»; экспорт 200 на Продвинутом и гейт на Пробном. Побочно вскрылось: **контейнер бэкенда отстал от кода на 6 маршрутов** (нет экспорта, `review-offers`, health-проб) и **`frontend/.env.local` смотрел на мёртвый порт 8013**.
- 2026-07-28 — **Сверка плана с кодом: план отставал на двое суток и на целый пласт работы.** Проверял не по плану, а прогоном: 975 тестов, `tsc`+`lint`+`build` фронта, живой стек, запросы к БД и API. Найдено: (1) **тринадцать коммитов не были учтены ни одним блоком** — почтовый слой, восстановление и смена пароля, подтверждение email, вход через ВК, привязка Телеграма, три раунда атакующих тестов и аудит безопасности тремя агентами; Блок 14 всё это время утверждал «не сделано», хотя работало вторые сутки → заведён **Блок 19** задним числом, Блок 14 закрыт; (2) **маркер `<!-- BLOCKS:END -->` был потерян** — `new_block.sh` не работал, новые блоки нельзя было завести скриптом, восстановлен; (3) статусы в MASTERPLAN и INDEX разошлись на пяти блоках (07, 08, 12, 13, 17), у Блока 08 стояли **два** взаимоисключающих статуса подряд — выровнено по факту; (4) падал `test_every_device_has_at_least_two_aliases`: незакоммиченный досев словаря добавил 14 устройств с одним алиасом, нарушив контракт Блока 17 Фазы 5 — досеяны вторые алиасы (кириллическая транслитерация бренда, как у `хонор`/`рилми`/`виво`), коллизий ноль, точность матчинга не изменилась (0,32%). Главный вывод по продукту: **живых цен в системе нет** — в БД 308 демо-офферов, боевой обход магазинов не запускался ни разу; и **мониторинг товаров, на котором построена вся тарифная сетка, недоступен пользователю** — бэкенд полный, фронта нет вовсе → заведён **Блок 20**.
- 2026-07-26 — **Блок 17 Фазы 2–3 исполнены.** Фаза 2: новый статус `review` (миграция `b1c2d3e4f5a6`) — «распознан part, канон не собрать» перестал молча падать в `unmatched` (живой siriust: unmatched 61.8%→0%, review→61.8%). Диагностика показала, что «мёртвая середина» структурна (кандидат требует product→device), а не пороговая. Фаза 3: data-driven расширение сида `DEVICES` (+~65 устройств: все старые iPhone, Samsung по MPN, Xiaomi/Honor/Realme/Infinix/Tecno) — auto на общем сэмпле **38.2%→64.7%**, на старом **17.5%→62.5%**; CLI `gaps` для дальнейшего пополнения. Тесты +3 (idem., все зелёные). Остаётся: выставить `review` в API модерации, замер по 4 остальным магазинам, полная петля gaps→автокандидаты.
- 2026-07-26 — **Заведён Блок 17 «Матчинг v2» по итогам первого живого замера.** Живой скрейп siriust (68 реальных заголовков, read-only) показал разрыв фикстуры↔реальность: auto 38.2% / candidate 0.0% / unmatched 61.8% против «100%» на фикстурах (Блок 10). Две структурные проблемы: (1) словарь устройств покрывает в осн. iPhone — все Samsung/Xiaomi уходят в unmatched (`device_id=None` при верном part_type); (2) «мёртвая середина» скоринга (`_INCOMPLETE_CORE_FACTOR`) делает band 0.5–0.8 недостижимым → очередь модерации обходится. Попутно исправлен баг парсеров `profi`/`tgsm`: `stock_status="out_of_stock"` вне CHECK → `"out"`. Всё занесено в `MANUAL_VERIFICATION.md`.
- 2026-07-25 — **Аудит состояния (6 агентов) + дорожная карта: заведены блоки 09–15.** Вывод аудита: инженерная база (схема, тесты, CI, auth) в порядке, но **продукта пока нет** — нет данных (парсеры), нет матчинга (сравнение цен между магазинами — суть продукта — не реализовано), ядро фронта (поиск/карточка) статично, платный доступ не гейтится вообще (`require_active_subscription` — мёртвый код), развернуть нечем (нет Dockerfile фронта, worker'а в compose, TLS). Отдельно при сверке с ТЗ/`subscriptions.md` найдено: тарифы продают **«мониторинг товаров» (10/100/500) и уведомления о снижении цены** — обеих сущностей в коде **нет вообще**; тексты на `/tariffs` противоречат спецификации (2/15+/50+ поставщиков против «5 на всех тарифах», «безлимит» против 500, «10 дней» против 7). Всего 89 задач (31 P0). Рекомендуемый порядок: 09 → 10 → 11 (рабочее ядро на импортированных данных, сеть не нужна) → 12/13 (коммерция) → 14/15 (запуск) → 07 (парсеры, нужна сеть).
- 2026-07-25 — **Второй раунд ревью правок («ship-with-nits»): ещё 6 дефектов исправлено** — rate-limit за прокси (XFF под флагом с безопасным дефолтом), порядок CORS/rate-limit (429 без CORS-заголовков), дубль активной подписки, история на каждой странице пагинации, `IndexError` при `max_requests<=0`, refresh деактивированного юзера. Плюс `close_redis()` + lifespan, индекс `(user_id, created_at DESC)`. **Бэкенд стал ruff-clean целиком (118 файлов)** → CI-job `lint` сделан блокирующим. Тесты: 161.
- 2026-07-25 — **Блок 03 (бэкенд) ЗАКРЫТ: 45 → 156 тестов.** 5 агентов параллельно (изолированные БД `bscout_test_1..5`) написали и сами прогнали 111 тестов: rate-limit, auth.service, search-history+RedisCache, deps-guards, API-контракты. **Тесты вскрыли 6 настоящих багов прод-кода — все исправлены** (CRITICAL: троттлинг отдавал 500 вместо 429; HIGH: регистрозависимый email, история поиска не писалась, истёкшая подписка считалась активной; MEDIUM: MultipleResultsFound в payment; LOW: логин деактивированного). Добавлены регресс-тесты на каждый фикс, миграция `c4115bee8c5e` (email→CITEXT).
- 2026-07-25 — **Порты и деплой-конфиг (решение владельца).** Прод остаётся на 8000; локально всё разведено через gitignored `.env` (BACKEND_PORT=8010, pg 5434). `docker-compose.yml` параметризован (+ `JWT_SECRET_KEY`/`DEBUG` из окружения), фронт-прокси через `BACKEND_URL`. Заведён CI `.github/workflows/ci.yml` (pg+redis сервисы, `alembic upgrade head` с нуля, unit → полный прогон с `--cov`; lint пока `continue-on-error`). Бэкенд-работа вынесена на ветку `feature/backend-core`.
- 2026-07-25 — **Финальное ревью (отдельный агент) 02/05/06/04/08: «ship-with-nits».** Подтверждены: чистый import-граф (arq/bs4 не ломают `src.main`), корректность Redis-локов и concurrency (сессия-на-парсер), fuzzy-SQL (index-eligible + injection-safe), миграция/Docker/worker. **Исправлен MEDIUM:** `get_active_subscription` фильтровал только `is_active` (не `end_date`) → просроченная подписка вечно давала платные фичи; добавлен `end_date > now()` + регресс-тест (46 тестов). Низкие нити (оставлены осознанно): `release_lock` без owner-token (гонка при TTL-истечении лока), `parse_price("1.299")`→1.299 (RU-формат: точка=десятичная), `full_sync` не реализован (dead-параметр), stale «running» до `STATUS_TTL` после краша, NOT NULL без default в миграции (ок на пустой таблице).
- 2026-07-25 — **Автономный проход 02–08.** Закрыты и проверены на живой БД/Redis: 02 (ядро данных), 06 (каркас парсеров), 05 (fuzzy). Тесты 17→45 (Блок 03 продвинут TDD-ом). Написаны, но не запущены (нет сети): 04 (Docker), 08 (arq-worker). Пропущен 07 (5 парсеров — нужны сеть+живые скрейпы). Всё на ветке `feature/frontend-integration`. Ограничение среды: `pip install`/`npm install`/`docker build` заблокированы → 04/07/08 не верифицируемы рантаймом.
- 2026-07-25 — **Блок 08 [~]** (arq-worker): `src/worker.py` (WorkerSettings + cron каталог 03:00 / цены 6ч → `parser_service.run_all`); `arq` в requirements. Запуск не проверен (нет `arq`).
- 2026-07-25 — **Блок 04 [~]** (Docker): `backend/Dockerfile` (pip, не poetry) + `.dockerignore` + сервис `backend` в compose (healthcheck pg). `docker build` не запускался (нет сети).
- 2026-07-25 — **Блок 05 ЗАКРЫТ** (fuzzy pg_trgm): `product_service.search(fuzzy=True)` через оператор `%` + `similarity()`-сортировка; гейтинг `is_fuzzy_enabled` (только advanced); контроллер вычисляет fuzzy по подписке. +2 теста (42 всего).
- 2026-07-25 — **Блок 06 ЗАКРЫТ** (каркас парсеров): exceptions, utils (parse_price/normalize_name/safe_request/compare_products), `ParserService` (upsert по `(store_id,source_sku)`, история цен, Redis status/locks, run_all с изоляцией), реальный контроллер. +18 тестов (40 всего).
- 2026-07-25 — **Ревью Блока 02 (2-й агент) нашло HIGH-баг миграции**: `drop_constraint` по старым именам FK (`products_store_id_fkey`) падал на чистом `alembic upgrade head`, т.к. `naming_convention` заставляет initial-миграцию создавать FK как `fk_products_store_id_stores`. Исправлено (op.f-имена в up/down). Заодно починен pre-existing баг: initial-downgrade не дропал тип `planenum` → `DROP TYPE IF EXISTS`. Полный from-base round-trip (down base → up head) теперь чист. Также убран лишний `joinedload` в `search`.
- 2026-07-25 — **Блок 02 ЗАКРЫТ** (автономно). Реструктуризация ядра: catalog-справочники (10 таблиц) + идемпотентный сид, `store_offers`/канон-`products`/`clusters`/`offer_price_history`/`match_candidates`, миграция `336d8cc4267a` (round-trip чисто, extensions+GIN+partial+CHECK+uniq), адаптация products/search/admin. Реестр моделей `db_metadata.py`. 22 теста зелёные (+4 новых). Проверено на живой БД. Сделано на ветке `feature/frontend-integration`.
- 2026-07-25 — Наполнен Блок 02 (Ядро данных: канон/offers/справочники/матчинг). Подтверждено: таблицы `products`/`price_history` пусты (0 строк) → реструктуризация чистая, до парсеров. Целевая схема (offer↔канон, справочники, `clusters`, `match_candidates`, опт-цена, enum-наличие, `uniq(store_id, source_sku)`) сверена со стандартами. Зафиксированы границы с блоками 05 (pg_trgm/GIN заводятся в 02) и 06 (upsert/`ParseResult` остаются в 06); 02 идёт ДО 05/06/07 и переименовывает их цели. Активный блок остаётся 01.
- 2026-07-25 — Заведён Блок 03 (покрытие тестами) [~]: по стандарту test-pyramid составлен план unit/integration/e2e/фронт/CI. Развёрнут каркас `backend/tests/{unit,integration}/` (pytest + pytest-asyncio + cov, маркеры, изоляция слоёв, тест-БД `bscout_test`, graceful skip) — 17 seed-тестов зелёные. Активный блок остаётся 01.
- 2026-07-25 — Внесены блоки 04–08 (добор бэкенда по итогам сверки со Спринтом 1–2: Docker, fuzzy pg_trgm, каркас парсеров, 5 парсеров, arq-планировщик). В INDEX добавлена строка `S1` [x] (Спринт 1 закрыт) и строка Блока 02. Источник задач — `docs/sprints/sprint-001-backend-api.md`.
- 2026-07-25 — Блок 01: реализованы Фазы 1–4 (auth e2e, Header+guards, кабинет+settings+history, подписка/тарифы). ~20 файлов фронта. Адверсариальное ревью 6 агентами (types/contracts — 0 находок), находки исправлены (RequireAdmin, failAuth, .btn-danger, unoptimized, alias). Backend-тесты 17 passed + починен баг тест-инфры (integration conftest). Не закрыт DoD: `npm run build/lint` (нет node_modules). Вопросы — в секции блока.
- 2026-07-25 — Заведён Блок 01 (интеграция фронта с бэком). Фаза 0 (верификация бэка) закрыта: auth-flow подтверждён руками на `:8001`, JSON snake_case, подписка без данных → 404. Ветка `feature/frontend-integration`.
- 2026-07-25 — Инициализирована система планирования (MASTERPLAN + INDEX + archive + MANUAL_VERIFICATION + `scripts/plan/` + SessionStart-хук).
