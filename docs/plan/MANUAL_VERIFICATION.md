# BScout — Ручная верификация

> То, что автотестами не подтвердить — у проекта **нет тестового набора**, поэтому этот
> слой несёт больше веса, чем обычно. Пункты **не удаляются** — только меняют статус.
> ⬜ не проверено · ✅ проверено (дата) · ❌ не прошло
> Коммит, добавивший ⬜-пункты, должен содержать строку-трейлер `Manual-verification:`.

## Поверхности BScout, требующие ручной проверки

### Frontend-интеграция (Блок 01) — требует `npm install` + запущенный бэк на :8000
- ⬜ `npm install && npm run build && npm run lint` из `frontend/` проходят без ошибок (в сессии не запускались — нет `node_modules`).
- ⬜ Логин: неверные креды → сообщение «Неверный email или пароль»; верные → редирект на `/account`, Header показывает Кабинет/Выйти.
- ⬜ Регистрация: валидация (пароль ≥6, совпадение паролей, согласие) → успех логинит и редиректит на `/account`.
- ⬜ Guard: аноним на `/account`, `/subscription`, `/admin` → редирект на `/login`. Не-админ на `/admin` → редирект на `/account`.
- ⬜ Кабинет: профиль/подписка/история подтягиваются из API; при отсутствии подписки — «нет активной подписки»; `settings` сохраняет профиль (`PATCH /users/me`).
- ⬜ Подписка: смена тарифа (`POST /payment/subscribe`) и отмена (`POST /payment/cancel`) отражаются в UI после инвалидации.
- ⬜ Выход (`useLogout`) чистит localStorage и возвращает Header в состояние Войти/Регистрация; кросс-таб `storage`-sync работает.

### Docker / порты (Блок 04) — требует сети
- ⬜ `docker build ./backend` проходит; `docker compose up -d` поднимает pg+redis+backend, миграции применяются на старте контейнера.
- ⬜ Локально сервисы слушают порты из `bsout-pricer/.env` (backend **8010**, pg 5434); без `.env` (прод) — 8000/5432/6379. Ничего не конфликтует с чужими проектами.
- ⬜ Фронт (`npm run dev`) через `BACKEND_URL=http://localhost:8010` реально ходит в свой бэкенд (а не в чужой сервис на 8000).
- ⬜ Прогон всех эндпоинтов в контейнере (curl/httpx).

### Парсеры
- ✅ 2026-08-08 Реальный скрейп каждого магазина не падает на текущем HTML — четыре доступных магазина прошли живой прогон по 50 позиций, `skipped: 0`, ошибок нет (ГринСпарк не проверялся: бот-защита). Структура сайтов меняется без предупреждения, поэтому пункт стоит перепроверять.
- ⬜ Нормализация имён (`Product.normalized_name`) корректно группирует одинаковые позиции разных магазинов. *(устарело: архитектура ушла на `clusters` + `canonical_key` + tier — см. пункт ниже)*
- ⬜ **Матчинг подтверждён только на фикстурах (block07):** на живых данных БД сейчас 308 offers → 100% `match_status='auto'`, `match_confidence`=1.0 у 307/308, 0 `ambiguous`. На реальном скрейпе проверить: (а) доля auto/review/reject реалистична (дизайн-гипотеза ~85% auto); (б) очередь `match_candidates` реально наполняется и модерация меняет привязку; (в) tier-разделение (Оригинал/OEM/Копия/Сервисный внутри одного кластера) не даёт ложных склеек и не дробит один и тот же товар.

### Платежи и подписки
- ⬜ «Показанная цена == списанная сумма» на реальном платёжном флоу.
- ⬜ Идемпотентность платежа: повторный webhook не создаёт второе списание / вторую подписку.
- ⬜ Гейтинг платных фич по тарифу (`trial` / `basic` / `advanced`) реально блокирует доступ (middleware `subscription_guard`).

### Кэш и данные
- ⬜ Redis-кэш поиска не отдаёт устаревшую цену после обновления парсером.
- ⬜ `RateLimitMiddleware` (120 req / 60s) не режет легитимный трафик и корректно ограничивает превышение.

## Журнал проверок

| Дата | Пункт | Результат | Кто |
|------|-------|-----------|-----|
| 2026-07-25 | Блок 02: миграция `336d8cc4267a` round-trip up→down→up на живой БД (:5434) | ✅ без ошибок | agent |
| 2026-07-25 | Блок 02: extensions citext/pg_trgm, 6 GIN-trgm/partial индексов, CHECK+uniq на `store_offers` | ✅ подтверждено запросами к pg_catalog | agent |
| 2026-07-25 | Блок 02: `/api/health`→200, `/api/products` без токена→403 (ASGI in-process) | ✅ | agent |
| 2026-07-25 | Блок 03: полный `pytest` на живых Postgres+Redis — **156 passed** | ✅ | agent |
| 2026-07-25 | Миграции: `downgrade base` → `upgrade head` с нуля (включая `c4115bee8c5e` email→CITEXT) | ✅ без ошибок | agent |
| 2026-07-25 | Порты: `docker compose config` — локально 8010/5434/6379, без `.env` (прод) 8000/5432/6379 | ✅ | agent |
| 2026-07-25 | Блок 02 спот-чек данных: кластер «Дисплей iPhone 13» корректно разбит на 4 канона по tier (Оригинал/OEM/Копия/Сервисный), Original≠Копия не склеены; 93/95 кластеров мультимагазинные; опт заполнен 54.5%, наличие — все 5 enum | ✅ на фикстурах | agent |
| 2026-07-26 | **Живой скрейп siriust (Профи), 68 реальных заголовков → matcher: auto 38.2%, candidate 0.0%, unmatched 61.8%** (на фикстурах было 100% auto). Вывод: (1) прогноз ~85% авто держится ТОЛЬКО при богатых словарях — сид из 57 устройств покрывает в осн. iPhone, все Samsung/Xiaomi ушли в unmatched (dev=None, но part_type извлекается верно); (2) **очередь модерации (0.5–0.8) фактически недостижима** — без device `_INCOMPLETE_CORE_FACTOR=0.5` роняет score до ≤0.25, реальные позиции молча падают в unmatched вместо review | ❌ вскрыло 2 проблемы | agent |
| 2026-07-26 | Баг: парсеры `profi`/`tgsm` возвращали `stock_status="out_of_stock"`, которого нет в CHECK `ck_store_offers_stock_status_allowed` (там `out`) → живой скрейп out-товара падал бы. Исправлено на `"out"` (3 места) + обновлены 2 unit-теста | ✅ исправлено | agent |
| 2026-07-26 | **Блок 17 Фаза 2:** статус `review` для «распознан part/device, канон не собрать» (миграция `b1c2d3e4f5a6`). Живой замер siriust (68, транзакция+rollback): `unmatched` 61.8%→0%, `review` 0→61.8% (42) — бэклог модерации наполняется вместо молчаливой потери. Полный сюит **702 passed** (+1 review-тест) | ✅ | agent |
| 2026-07-26 | **Блок 17: `review`-бэклог выставлен в API модерации** — `GET /api/admin/review-offers` (пагинация, admin-only); резолв через существующий `POST /offers/{id}/link`. Тесты модерации 20 passed (+2 новых). Роут зарегистрирован, app импортируется | ✅ | agent |
| 2026-07-26 | **Блок 17 Фаза 3:** сид `DEVICES` расширен (+~65 устройств по gap-анализу живого siriust; MPN+маркетинг-алиасы; идемпотентно). Живой замер siriust: общий сэмпл (68) **auto 38.2%→64.7%**, `unmatched`→0; старый сэмпл (251) **17.5%→62.5%**. CLI `gaps` для дальнейшего пополнения | ✅ | agent |
| 2026-07-26 | **Блок 17 Фаза 4:** живой замер auto-доли по всем магазинам (read-only extract, после Фазы-3 словаря): siriust 64.7% · divizion 59.6% (n=925) · liberti 41.3% (n=1123) · tgsm 33.3% (n=120). `unmatched`=0% везде — всё нераспознанное в `review`. Greenspark — нет живого парсера (бот-защита) | ✅ | agent |
| 2026-07-26 | **Блок 17 Фаза 3 (2-й gaps-проход):** досев словаря по `gaps`-хвостам divizion/liberti/tgsm (+~48 старых моделей: Honor 8/9/10/20/30/200, Redmi 4/5/7/Note 4X/5A, Realme C2/C3/6S/8i, Oppo A5/A17/A53/A74, Samsung A05s/A20s). Живой замер до→после: **divizion 59.6%→82.9%**, **liberti 41.3%→79.3%**, **tgsm 33.3%→50.0%**; `unmatched`≈0. Словарь 389 устройств. Тесты 64 зелёные | ✅ | agent |
| 2026-07-26 | **Блок 17: досев tgsm-хвоста через gaps** (+~33 устройства: HTC, Vivo V, ZTE, Meizu, Alcatel, Lenovo A, планшеты Galaxy Tab SM-T/iPad/Asus, старые Samsung i9000/i8190, Poco F2 Pro; +5 брендов). tgsm auto: общий сэмпл **50.0%→66.7%**, сложный **25.0%→58.9%**. Словарь 429 устройств. Тесты зелёные | ✅ | agent |
| 2026-07-26 | Остаётся (Блок 17): потолок tgsm — остаток хвоста настоящий мусор (China Mobile/Nokia N95, кнопочные Samsung B/C); greenspark-парсер (бот-защита); полная авто-петля `gaps`→кандидаты-в-модерацию | ⬜ | agent |
| 2026-07-29 | **Качество разбора на реальном HTML магазинов** (офлайн, фикстуры `tests/fixtures/parsers/`, 118 записей): обязательные поля (sku/title/цена>0/`stock_status` в CHECK/url) — **118/118**; наличие распознано **85/118 (72%)**, опт **40/118 (34%)**, категория **40/118 (34%)**. Узкое место — Либерти: на 33 из 37 позиций раздела наличия нет (`no stock info for listing item`) | ✅ замерено | agent |
| 2026-07-29 | **Блок 08: асинхронный запуск парсера** — проверено на живом стеке (бэкенд с новым кодом на :8011) **без запуска воркера, то есть без обращений к магазинам**: `POST /api/admin/parsers/run {"full_sync":true}` → **202 за 62 мс** с `job_id`; в Redis `arq:queue`=1 и ключ `arq:job:<тот же id>`; `GET /api/admin/parsers` → `is_running: true` только у tgsm; `arq src.worker.WorkerSettings` → `Starting worker for 12 functions: run_parser, …`, остановлен с `0 jobs complete`. Тестовые задачи из Redis удалены | ✅ | agent |
| 2026-07-29 | Остаётся (Блок 08): **боевой прогон по реальным магазинам так и не делался**. Теперь есть дешёвый первый шаг — запуск из админки с лимитом 25–100 позиций (вместо полного обхода на 3–4 часа). Нужно согласие владельца | ⬜ | agent |
| 2026-08-08 | **Первый живой прогон четырёх магазинов, лимит 50 на магазин** (через `run_isolated` — тот же путь, что дёргает воркер из админки; образы backend+worker пересобраны, оба `healthy`). Везде `status: done`, `skipped: 0`, ошибок нет, `deactivated: 0` (при лимите деактивация намеренно заблокирована — защита сработала). `auto`/`review`/`unmatched`: **Profi с `section=zapchasti` 50/0/0**, Profi без фильтра 22/4/24, Divizion с `zapchasti` 41/3/0, Liberti 13/32/5, **TGSM 0/16/34**. Живых офферов в БД **402 → 624**. Время 31–83 с на магазин | ✅ | agent |
| 2026-08-08 | **Фикс discovery Profi подтверждён на живых данных** — раньше ограниченный прогон возвращал 0 товаров (первые ссылки sitemap были категориями), теперь 50 из 50 | ✅ | agent |
| 2026-08-08 | **Опт подтверждён только у Профи.** Разделение офферов по происхождению (живой скрейп против демо-сида) даёт: опт заполнен у profi 78/78 живых, divizion 0/138, liberti 0/50, tgsm 0/50. Весь опт у остальных магазинов — демо-записи сида (`lb…`, `dv-…`, `tg-…`), у ГринСпарка живого парсера нет. Согласуется с тем, что у Либерти опт за авторизованным кабинетом. ⚠️ Промежуточный вывод «опт есть у всех пяти» был ошибкой: считали по всей таблице, не отделив сид от живых данных | ✅ перемерено | agent |
| 2026-08-08 | **Наличие у Либерти при разборе карточек читается у 100% позиций** (50/50, ноль `unknown`). Прежняя запись про 89% без наличия касалась ЛИСТИНГА (`no stock info for listing item`) — оба факта верны, проблема уже, чем записывалась | ✅ перемерено | agent |
| 2026-08-08 | Остаётся (Блок 07): **TGSM даёт 0% `auto`** — OpenCart, все ссылки вида `index.php?route=product/product&product_id=N`, фильтр `section` структурно неприменим, прогон по sitemap набирает аксессуары. Нужен обход по категориям через `path=` | ⬜ | agent |
| 2026-07-29 | **Прод-nginx не поднимется:** `docker-compose.prod.yml:296` монтирует `./deploy/nginx/bscout.conf`, которого нет в репозитории (есть только `deploy/nginx/nginx.conf`). Там же должен быть `proxy_read_timeout` — Блок 15 | ❌ найдено, не чинилось | agent |

## Мониторинг и уведомления во фронте (Блок 20, 2026-07-29)

Проверено в браузере на живой БД, отдельным тестовым аккаунтом (заведён и удалён после проверки; демо-цена офера 23 возвращена к исходной).

- ✅ 2026-07-29 «Отслеживать» в карточке выдачи и на карточке товара: клик → «Отслеживается», состояние переживает перезагрузку, повторный клик снимает.
- ✅ 2026-07-29 Лимит тарифа: на 11-м товаре **402** с текстом «Достигнут лимит тарифа «Пробный»: отслеживается 10 из 10 товаров» и кнопкой «Повысить тариф» — не голая «ошибка».
- ✅ 2026-07-29 Кабинет `/account/tracking`: счётчик 10/10 с полосой, список с текущей ценой, дельтой и числом магазинов; целевая цена 5 500 ₽ сохранена через `PATCH` и отображается.
- ✅ 2026-07-29 Уведомления: снижение 3130 → 2330 ₽ через `AlertService.scan_for_drops` породило запись; колокольчик в шапке показал **1**, страница показала «Цена снизилась» с переходом 3 130 ₽ → 2 330 ₽; «Прочитано» погасило бейдж.
- ✅ 2026-07-29 Экспорт `tracking.csv`: на Продвинутом — 200 и файл; на Пробном — «Экспорт доступен на тарифе «Продвинутый»» со ссылкой на тарифы.
- ✅ 2026-07-29 Мобильная вёрстка (375×812): строки мониторинга складываются в столбец, ничего не уезжает за экран.
- ❌ 2026-07-29 **Контейнер `bscout-backend` отстал от кода**: 52 маршрута против 58 — нет `/api/export/*`, `/api/admin/review-offers`, `/api/health/live|ready`. Экспорт проверялся на локальном uvicorn с актуальным кодом. Нужно `docker compose up -d --build backend`.
- ❌ 2026-07-29 **`frontend/.env.local` указывал на `:8013`** — порт мёртв, фронт локально не видел бэкенд вообще. Исправлено на `:8010`.
- ⬜ Предупреждение гидратации на `/search` (`Expected server HTML to contain a matching <button>`): воспроизводится и без новой кнопки, причина — `Suspense` вокруг `useSearchParams` (сервер отдаёт `SearchFallback`). Отдельной задачей.
- ⬜ **Флаки-тест:** `test_attack_password_change_is_not_throttled` упал 1 раз из 3 полных прогонов (в одиночку — зелёный). Симптом: ни одного 429 на 30 попыток, то есть строгий лимит не сработал. Похоже на fail-open рейт-лимитера при ошибке Redis (`_evaluate` глотает `REDIS_ERRORS`) на фоне пересоздания singleton между тестами. В CI даст плавающую красноту.

## Матчинг на живых данных — точность (2026-07-28)

- ✅ 2026-07-28 Замер точности на 395 живых названиях Дивизиона: неверных сопоставлений **6% → 2%** после досева вариантов Pro/Plus/Ultra. Распознавание устройства 82% → 85%.
- ✅ 2026-07-28 Проверено, что `Дисплей для Tecno Spark 40 Pro` больше не уходит на `Tecno Spark 40`.
- ✅ 2026-07-28 Точность замерена по всем доступным магазинам: Дивизион 2,5% неверных, Либерти 1,1%, Профи 0%, ТГСМ 0%. ⚠️ Выборки Профи (54) и ТГСМ (30) малы — цифры шумные.
- ❌ 2026-07-28 ГринСпарк измерить невозможно: сайт закрыт бот-защитой. Нужна договорённость с магазином о выгрузке.
- ✅ 2026-07-28 Отчёт точности встроен в петлю (`python -m src.cli precision`), режим CI с порогом проверен: код 0 при 0,32% против порога 5%, код 1 против порога 0,1%.
- ✅ 2026-07-28 CI достроен и подключён к метрике точности; шаги воспроизведены локально. ⚠️ Сам workflow на GitHub не запускался — проверено покомандно, а не прогоном Actions.
- ⬜ Проверить числовые коллизии: `11"` (диагональ) читается как номер модели Xiaomi 11. Досев чинит частные случаи, класс ошибки остаётся.
- ⬜ Проверить, что после досева словаря не появились ложные склейки между близкими моделями (нужен ручной просмотр выборки `auto`-сопоставлений).
- ⬜ Мультимодельные предложения (Блок 18): убедиться, что после реализации `offers_count` и `stores_count` не задваиваются.

