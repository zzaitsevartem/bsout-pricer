# BScout — Ручная верификация

> То, что автотестами не подтвердить — у проекта **нет тестового набора**, поэтому этот
> слой несёт больше веса, чем обычно. Пункты **не удаляются** — только меняют статус.
> ⬜ не проверено · ✅ проверено (дата) · ❌ не прошло
> Коммит, добавивший ⬜-пункты, должен содержать строку-трейлер `Manual-verification:`.

## Поверхности BScout, требующие ручной проверки

### Frontend-интеграция (Блок 01) — требует `npm install` + запущенный бэк на :8000
- ⬜ `npm install && npm run build && npm run lint` из `frontend/` проходят без ошибок (в сессии не запускались — нет `node_modules`).
- ⬜ Логин: неверные креды → сообщение «Неверный email или пароль»; верные → редирект на `/account`, Header показывает Кабинет/Выйти.
- ⬜ Регистрация: валидация (пароль ≥6, совпадение паролей, согласие) → успех логинит и редиректит на `/account`.
- ⬜ Guard: аноним на `/account`, `/subscription`, `/admin` → редирект на `/login`. Не-админ на `/admin` → редирект на `/account`.
- ⬜ Кабинет: профиль/подписка/история подтягиваются из API; при отсутствии подписки — «нет активной подписки»; `settings` сохраняет профиль (`PATCH /users/me`).
- ⬜ Подписка: смена тарифа (`POST /payment/subscribe`) и отмена (`POST /payment/cancel`) отражаются в UI после инвалидации.
- ⬜ Выход (`useLogout`) чистит localStorage и возвращает Header в состояние Войти/Регистрация; кросс-таб `storage`-sync работает.

### Docker / порты (Блок 04) — требует сети
- ⬜ `docker build ./backend` проходит; `docker compose up -d` поднимает pg+redis+backend, миграции применяются на старте контейнера.
- ⬜ Локально сервисы слушают порты из `bsout-pricer/.env` (backend **8010**, pg 5434); без `.env` (прод) — 8000/5432/6379. Ничего не конфликтует с чужими проектами.
- ⬜ Фронт (`npm run dev`) через `BACKEND_URL=http://localhost:8010` реально ходит в свой бэкенд (а не в чужой сервис на 8000).
- ⬜ Прогон всех эндпоинтов в контейнере (curl/httpx).

### Парсеры
- ⬜ Реальный скрейп каждого магазина не падает на текущем HTML (структура сайтов меняется без предупреждения).
- ⬜ Нормализация имён (`Product.normalized_name`) корректно группирует одинаковые позиции разных магазинов. *(устарело: архитектура ушла на `clusters` + `canonical_key` + tier — см. пункт ниже)*
- ⬜ **Матчинг подтверждён только на фикстурах (block07):** на живых данных БД сейчас 308 offers → 100% `match_status='auto'`, `match_confidence`=1.0 у 307/308, 0 `ambiguous`. На реальном скрейпе проверить: (а) доля auto/review/reject реалистична (дизайн-гипотеза ~85% auto); (б) очередь `match_candidates` реально наполняется и модерация меняет привязку; (в) tier-разделение (Оригинал/OEM/Копия/Сервисный внутри одного кластера) не даёт ложных склеек и не дробит один и тот же товар.

### Платежи и подписки
- ⬜ «Показанная цена == списанная сумма» на реальном платёжном флоу.
- ⬜ Идемпотентность платежа: повторный webhook не создаёт второе списание / вторую подписку.
- ⬜ Гейтинг платных фич по тарифу (`trial` / `basic` / `advanced`) реально блокирует доступ (middleware `subscription_guard`).

### Кэш и данные
- ⬜ Redis-кэш поиска не отдаёт устаревшую цену после обновления парсером.
- ⬜ `RateLimitMiddleware` (120 req / 60s) не режет легитимный трафик и корректно ограничивает превышение.

## Журнал проверок

| Дата | Пункт | Результат | Кто |
|------|-------|-----------|-----|
| 2026-07-25 | Блок 02: миграция `336d8cc4267a` round-trip up→down→up на живой БД (:5434) | ✅ без ошибок | agent |
| 2026-07-25 | Блок 02: extensions citext/pg_trgm, 6 GIN-trgm/partial индексов, CHECK+uniq на `store_offers` | ✅ подтверждено запросами к pg_catalog | agent |
| 2026-07-25 | Блок 02: `/api/health`→200, `/api/products` без токена→403 (ASGI in-process) | ✅ | agent |
| 2026-07-25 | Блок 03: полный `pytest` на живых Postgres+Redis — **156 passed** | ✅ | agent |
| 2026-07-25 | Миграции: `downgrade base` → `upgrade head` с нуля (включая `c4115bee8c5e` email→CITEXT) | ✅ без ошибок | agent |
| 2026-07-25 | Порты: `docker compose config` — локально 8010/5434/6379, без `.env` (прод) 8000/5432/6379 | ✅ | agent |
| 2026-07-25 | Блок 02 спот-чек данных: кластер «Дисплей iPhone 13» корректно разбит на 4 канона по tier (Оригинал/OEM/Копия/Сервисный), Original≠Копия не склеены; 93/95 кластеров мультимагазинные; опт заполнен 54.5%, наличие — все 5 enum | ✅ на фикстурах | agent |
| 2026-07-26 | **Живой скрейп siriust (Профи), 68 реальных заголовков → matcher: auto 38.2%, candidate 0.0%, unmatched 61.8%** (на фикстурах было 100% auto). Вывод: (1) прогноз ~85% авто держится ТОЛЬКО при богатых словарях — сид из 57 устройств покрывает в осн. iPhone, все Samsung/Xiaomi ушли в unmatched (dev=None, но part_type извлекается верно); (2) **очередь модерации (0.5–0.8) фактически недостижима** — без device `_INCOMPLETE_CORE_FACTOR=0.5` роняет score до ≤0.25, реальные позиции молча падают в unmatched вместо review | ❌ вскрыло 2 проблемы | agent |
| 2026-07-26 | Баг: парсеры `profi`/`tgsm` возвращали `stock_status="out_of_stock"`, которого нет в CHECK `ck_store_offers_stock_status_allowed` (там `out`) → живой скрейп out-товара падал бы. Исправлено на `"out"` (3 места) + обновлены 2 unit-теста | ✅ исправлено | agent |
| 2026-07-26 | **Блок 17 Фаза 2:** статус `review` для «распознан part/device, канон не собрать» (миграция `b1c2d3e4f5a6`). Живой замер siriust (68, транзакция+rollback): `unmatched` 61.8%→0%, `review` 0→61.8% (42) — бэклог модерации наполняется вместо молчаливой потери. Полный сюит **702 passed** (+1 review-тест) | ✅ | agent |
| 2026-07-26 | Остаётся (Блок 17): `review`-офферы не выставлены в API модерации (`list_candidates` читает только `match_candidates` через INNER JOIN Product) — бэклог виден в БД, но не в админ-очереди | ⬜ | agent |
