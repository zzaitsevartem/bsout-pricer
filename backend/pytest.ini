[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --strict-markers
markers =
    unit: fast, pure logic — no Postgres/Redis/network, always runnable
    integration: needs Postgres (bscout_test) and/or Redis up (docker compose up -d)
